# D1.Q1.3 Experiment - Minimum Analytics Event Set

## Experiment E1.7

Question: What is the minimum analytics event set for SM1-SM8, and which gap can be wired immediately?

Method:

- Read current analytics methods and call sites.
- Map SM1-SM8 to event, local DB, or audit artifact sources.
- Add the cheapest missing event for SM1: `onboarding_completed`.
- Verify event shape with `AnalyticsService` unit tests and analyzer.

Expected information gain: medium. This separates product telemetry from CI/content audit metrics.

Cost: 0.5-1 hour.

Immediate code target:

```text
onboarding_completed {level, goal}
```

## Experiment E1.8

Question: Can the event export harness compute SM1 funnel stages?

Method:

- Add `NorthStarFunnelEvaluator` for open -> onboarding -> first SRS.
- Add `tool/research/funnel_report.dart` for normalized or GA4-shaped event exports.
- Run the funnel report against the existing normalized event fixture.

Expected information gain: high. This makes the SM1 observability gap executable instead of only documented.

Command:

```text
dart run tool\research\funnel_report.dart --events docs\research\fixtures\north-star-events-e1.3.json --window-start 2026-05-01T00:00:00.000Z
```
