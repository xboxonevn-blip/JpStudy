# D1.Q1.4 Analysis - Real Export Funnel Baseline

## E1.9 Result

Real current funnel counts are no longer blocked by GCP identity or BigQuery Job User access. The hard blocker is now dataset availability: as of `2026-05-15`, `jpstudy-v2.analytics_536663906` still does not exist in either `asia-southeast1` or `US`.

The chosen path remains direct BigQuery query using the service account `ga4-data-reader@jpstudy-v2.iam.gserviceaccount.com` and `GOOGLE_APPLICATION_CREDENTIALS`. The handoff path was `C:\Users\xboxo\.config\gcp\jpstudy-v2-ga4-key.json`; the matching key present in this workspace is `C:\Users\xboxo\.config\gcp\jpstudy-v2-591716a5e835.json`.

The Python runner is unit-tested, but the local default `python` points to an Inkscape Python without `pip`. `tool/research/bigquery_rest_runner.js` is the current practical fallback: it mints the service-account token without third-party packages and runs BigQuery REST queries. On `2026-05-15`, `SELECT 1 AS ok` passed, `asia-southeast1` listed only `firebase_crashlytics`, `firebase_messaging`, and `firebase_sessions`, and `US` listed zero datasets. The GA4 event-count query returned `404` for `analytics_536663906` in both locations.

Backup path: GA4 Data API against property `536663906` under account `393943579`. Property Access is blocked by a UI bug, so defer the Data API path until BigQuery export is unavailable or delayed beyond the first export window.

## What The Query Covers

- SM1: open/session event -> `onboarding_completed` -> `srs_review_completed`
- NS review gate: `srs_review_completed`
- NS quiz gate: `n5_micro_quiz_completed`
- NS quality gate: `session_quality_rated`

## What The Query Avoids

- No names, emails, prompts, answers, content strings, or raw learner text.
- No full table export.
- No user properties beyond `user_id` / `user_pseudo_id`, which are required for cohort joining.

## Red Team

Steelman opposite: pseudonymous ids can still be sensitive when combined with event timing, so this is low-PII, not no-risk. Use aggregate reports for sharing. Keep raw JSON in an ignored secure location such as `docs/research/secure/`, and do not commit it.

## Action

Now:

1. Verify BigQuery service-account access:

```text
node tool/research/bigquery_rest_runner.js --query "SELECT 1 AS ok" --out docs/research/secure/bigquery-rest-smoke.json
```

Current verification note: this succeeds. Do not use `firebase_sessions.sessions_*` as the GA4 readiness probe; the current Firebase datasets can exist while still having no useful event tables.

Local runner note: `tool/research/bigquery_runner.py` is unit-tested, but the current default `python` has no `google-cloud-bigquery` SDK installed and no `pip`; use `tool/research/bigquery_rest_runner.js` unless a normal Python environment is configured.

2. Poll for the GA4 export dataset:

```text
node tool/research/bigquery_rest_runner.js --query "SELECT schema_name FROM `jpstudy-v2.region-asia-southeast1`.INFORMATION_SCHEMA.SCHEMATA ORDER BY schema_name" --out docs/research/secure/bigquery-datasets-asia-southeast1.json
node tool/research/bigquery_rest_runner.js --location US --query "SELECT schema_name FROM `jpstudy-v2.region-us`.INFORMATION_SCHEMA.SCHEMATA ORDER BY schema_name" --out docs/research/secure/bigquery-datasets-us.json
```

3. After `analytics_536663906` exists, run the 48h event-count query first:

```text
node tool/research/bigquery_rest_runner.js --sql tool/research/ga4_event_counts_last48h.sql --out docs/research/secure/ga4-event-counts-last48h.json
```

4. Then export GA4 rows for the reports:

```text
node tool/research/bigquery_rest_runner.js --sql tool/research/ga4_ns_export.sql --out docs/research/secure/ga4-ns-events.json
```

5. Feed the ignored JSON to both reports:

```text
dart run tool/research/north_star_report.dart --ga4-events docs/research/secure/ga4-ns-events.json --window-start <iso>
dart run tool/research/funnel_report.dart --ga4-events docs/research/secure/ga4-ns-events.json --window-start <iso>
```

Update the surprise journal when the first real NS result differs from the synthetic baseline by more than 20%. For now, record `analytics_536663906` absence as a readiness blocker, not a real-user NS datapoint.
