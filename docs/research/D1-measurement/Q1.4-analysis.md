# D1.Q1.4 Analysis - Real Export Funnel Baseline

## E1.9 Result

Real current funnel counts are no longer blocked by GCP identity or BigQuery Job User access. The chosen path is direct BigQuery query using the service account `ga4-data-reader@jpstudy-v2.iam.gserviceaccount.com` and `GOOGLE_APPLICATION_CREDENTIALS`. The handoff path was `C:\Users\xboxo\.config\gcp\jpstudy-v2-ga4-key.json`; the matching key present in this workspace is `C:\Users\xboxo\.config\gcp\jpstudy-v2-591716a5e835.json`.

The GA4 export dataset is expected at `jpstudy-v2.analytics_536663906`. It may not exist until GA4 creates the first export table after the first event export window, up to 24 hours. In the current local check, the service account can mint an OAuth token, list BigQuery datasets, and run `SELECT 1 AS ok`, but `firebase_crashlytics`, `firebase_messaging`, and `firebase_sessions` currently list zero tables; the requested `firebase_sessions.sessions_*` wildcard therefore matches no tables yet.

Backup path: GA4 Data API against property `536663906` under account `393943579`. Property Access is blocked by a UI bug, so defer the Data API path until BigQuery export is unavailable or delayed beyond the first export window.

## What The Query Covers

- SM1: open/session event -> `onboarding_completed` -> `srs_review_completed`
- NS review gate: `srs_review_completed`
- NS quiz gate: `n5_micro_quiz_completed`
- NS quality gate: `session_quality_rated`

## What The Query Avoids

- No names, emails, prompts, answers, content strings, or raw learner text.
- No full table export.
- No user properties beyond `user_id` / `user_pseudo_id`, which are required for cohort joining.

## Red Team

Steelman opposite: pseudonymous ids can still be sensitive when combined with event timing, so this is low-PII, not no-risk. Use aggregate reports for sharing. Keep raw JSON in an ignored secure location such as `docs/research/secure/`, and do not commit it.

## Action

Now:

1. Verify BigQuery service-account access:

```text
python tool/research/bigquery_runner.py --query "SELECT 1 AS ok FROM `jpstudy-v2.firebase_sessions.sessions_*` LIMIT 1"
```

Current verification note: that exact query currently fails with `does not match any table`. Use plain `SELECT 1 AS ok` to confirm job permission, then rerun the wildcard once `firebase_sessions` has tables.

Local runner note: `tool/research/bigquery_runner.py` is unit-tested, but the current default `python` has no `google-cloud-bigquery` SDK installed and no `pip`; install the SDK in a normal Python environment before using the runner.

2. After `analytics_536663906` exists, export GA4 rows:

```text
python tool/research/bigquery_runner.py --sql tool/research/ga4_ns_export.sql --out docs/research/secure/ga4-ns-events.json
```

3. Feed the ignored JSON to both reports:

```text
dart run tool/research/north_star_report.dart --ga4-events docs/research/secure/ga4-ns-events.json --window-start <iso>
dart run tool/research/funnel_report.dart --ga4-events docs/research/secure/ga4-ns-events.json --window-start <iso>
```

After 24 hours, run the first real GA4 NS report and update the surprise journal if the result differs from the synthetic baseline by more than 20%.
