# D1.Q1.3 Analysis - Minimum Analytics Event Set

## Event Matrix

| Metric | Minimum source | Current status | Gap |
| --- | --- | --- | --- |
| NS review gate | `srs_review_completed` | Wired | Needs real export sample |
| NS quiz gate | `n5_micro_quiz_completed` | Wired as N5 lesson 1-25 proxy | Needs explicit embedded quiz id |
| NS quality gate | `session_quality_rated` | Wired | Event-only; not durable if consent/export missing |
| SM1 funnel | app open/default GA4, `onboarding_completed`, first `srs_review_completed` | `onboarding_completed` added | Need DebugView/export verification |
| SM2 D1/D7/D30 retention | user continuity + dated learning events | Export-shaped scorer ready | Need real GA4 export |
| SM3 skill mastery velocity | Drift SRS/mastery tables + periodic aggregate export | Local providers exist | No analytics aggregate event yet |
| SM4 content quality score | content audit reports | Existing docs/reports partial | Needs refreshed D2 audit |
| SM5 performance budget | Lighthouse/build reports | Not user telemetry | Needs D7 baseline/budget |
| SM6 accessibility | axe/manual audit reports | Not user telemetry | Needs D6 audit |
| SM7 Vietnamese editorial coverage | content audit reports | Existing prior audit only | Needs D2/D3 rerun |
| SM8 critical path bugs | smoke/E2E reports + crash/error telemetry | Partial tests | Needs launch observability |

## E1.7 Result

`onboarding_completed {level, goal}` is now part of `AnalyticsService` and is called after onboarding preferences are persisted in `HomeScreen`.

## Hypothesis Update

- H1.3.1: supported. NS event families do not identify onboarding completion.
- H1.3.2: supported. The onboarding event was the cheapest SM1 gap to close.
- H1.3.3: supported for planning. SM5-SM7 should be CI/content audit artifacts, not Firebase user events.

## Red Team

Steelman opposite: because the HomeScreen widget integration test could not run cleanly, the call site is less strongly verified than the service method. Falsifier: Firebase DebugView or a real GA4 export row shows `onboarding_completed` with expected `level` and `goal` after completing onboarding.

## Action

Queue DebugView/live export verification when browser/manual telemetry access is available. Keep `onboarding_completed` in the minimum event set for SM1.

## E1.8 Result

The SM1 funnel now has an executable report over normalized or GA4-shaped event exports. Running it against `north-star-events-e1.3.json` shows why old fixtures cannot answer SM1: they contain first SRS events but no open/onboarding events.

## E1.8 Red Team

Steelman opposite: the funnel report can create false precision if event exports omit default GA4 open/session events or if user identity resets before first SRS. Falsifier: a real GA4 export where `app_open` or `session_start`, `onboarding_completed`, and `srs_review_completed` join under stable user ids.

## E1.8 Action

Use `tool/research/funnel_report.dart` as the first check when a real export sample exists. Until then, SM1 is instrumentation-ready but not measured.
