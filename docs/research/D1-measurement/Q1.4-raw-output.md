# D1.Q1.4 Raw Output - Real Export Funnel Baseline

## Source Facts

Official GA4 BigQuery schema source: https://support.google.com/analytics/answer/7029846

Relevant observed schema facts:

```text
Dataset name: analytics_<property_id>
Daily table: events_YYYYMMDD
Intraday table: events_intraday_YYYYMMDD
event_timestamp: microseconds UTC
event_name: event name
event_params: repeated RECORD with key and typed value fields
user_id: app-assigned user id
user_pseudo_id: pseudonymous app-instance/user id
```

## Local Access Output

Already observed in E1.5:

```text
npx firebase projects:list --json -> jpstudy-v2 visible
npx firebase apps:list --project jpstudy-v2 --json -> apps visible
gcloud -> not recognized
bq -> not recognized
```

## Artifact Created

```text
tool/research/ga4_ns_export.sql
tool/research/bigquery_runner.py
test/tool/research/bigquery_runner_test.py
tool/research/ga4_event_counts_last48h.sql
tool/research/bigquery_rest_runner.js
test/tool/research/bigquery_rest_runner_node_test.js
```

## BigQuery Access Check - 2026-05-14

Credential path supplied in the handoff was not present locally:

```text
C:\Users\xboxo\.config\gcp\jpstudy-v2-ga4-key.json -> missing
```

Matching service-account key found:

```text
C:\Users\xboxo\.config\gcp\jpstudy-v2-591716a5e835.json
client_email: ga4-data-reader@jpstudy-v2.iam.gserviceaccount.com
```

Python SDK status:

```text
python -> C:\Program Files\Inkscape\bin\python.exe
python -m unittest test.tool.research.bigquery_runner_test -> pass 4/4
google-cloud-bigquery -> not installed for this python
python -m pip install google-cloud-bigquery -> failed; Inkscape Python has no pip
C:\Users\xboxo\AppData\Local\Programs\Python\Python313\python.exe -> present, but stdlib is broken locally (`tempfile` missing)
```

REST verification using the same service account:

```text
OAuth service-account token mint -> ok
BigQuery job SELECT 1 AS ok -> ok, totalRows=1
Dataset list -> ok
Datasets visible -> firebase_crashlytics, firebase_messaging, firebase_sessions
firebase_crashlytics tables -> totalItems=0
firebase_messaging tables -> totalItems=0
firebase_sessions tables -> totalItems=0
analytics_536663906 -> 404 Not found
Requested query SELECT 1 FROM `jpstudy-v2.firebase_sessions.sessions_*` LIMIT 1 -> INVALID_ARGUMENT, no matching table
```

Interpretation: service-account auth and BigQuery Job User permission are working. The requested `firebase_sessions.sessions_*` read cannot confirm table-level data access yet because the wildcard currently matches no tables in this local check.

## BigQuery Recheck - 2026-05-15

The local default `python` remains unsuitable for the Python SDK path, so the
recheck used the Node REST runner. The runner mints an OAuth token from the
same service-account JSON and calls the BigQuery REST API directly.

```text
node tool/research/bigquery_rest_runner.js --query "SELECT 1 AS ok" --out docs/research/secure/bigquery-rest-smoke-2026-05-15.json
-> Wrote 1 rows
rows -> [{"ok":1}]
```

Dataset inventory:

```text
asia-southeast1 INFORMATION_SCHEMA.SCHEMATA
-> firebase_crashlytics
-> firebase_messaging
-> firebase_sessions

US INFORMATION_SCHEMA.SCHEMATA
-> 0 rows
```

GA4 event-count query:

```text
node tool/research/bigquery_rest_runner.js --sql tool/research/ga4_event_counts_last48h.sql
-> 404 Not found: Dataset jpstudy-v2:analytics_536663906 was not found in location asia-southeast1

node tool/research/bigquery_rest_runner.js --location US --sql tool/research/ga4_event_counts_last48h.sql
-> 404 Not found: Dataset jpstudy-v2:analytics_536663906 was not found in location US
```

Interpretation: credentials, OAuth, and BigQuery jobs are working. Real GA4
event counts remain blocked because `analytics_536663906` still does not exist
in either checked location.
