# Research Journal - 2026-05-14

## 2026-05-14T00:00:00+07:00

Active dimension: D1

Active question: Q1.2

Hypothesis being tested: H1.2.1 / H1.2.2 / H1.2.3

Experiment status: done

Commit hash: `8387e4440ed26aaea5caf1d51abb6023d234df18`

Surprise log: event-level replay was enough to satisfy the one-command simulator goal, but it is weaker than true Drift-backed replay.

Belief update: Phase 0 can regress the NS event contract across personas now; real beta readiness still depends on export access or app-level replay.

NS current value: simulator run reports 6.00% on 10 observed users with denominator 50.

Next 6h plan: pursue real GA4 export sample or app-level replay harness.

EXPERIMENT E1.6 - Synthetic persona event replay

H tested: H1.2.1, H1.2.2, H1.2.3

Method: wrote failing tests for deterministic persona-tagged event generation and CLI `--simulate-users`; implemented minimal event simulator that reuses the NS event mapper.

Result: one command scored 10 simulated users, NS 6.00%, observed users 10, qualified users 3 / 50.

Surprise: mild; this closes event-contract replay but not real Drift-backed app replay.

Update: Q1.2 has a deterministic event-level simulator; remaining stronger version is app-level/DB-backed replay.

Action: none / new experiment queued

Time: 0.75h

EXPERIMENT E2.5 - Content scope vs JLPT rough counts

H tested: H2.5.1, H2.5.2, H2.5.3

Method: added TDD-backed scope scanner for distinct vocab terms, kanji characters, grammar points, and grammar example sentences. Compared cumulative local counts against rough JLPT study-guide counts after checking that official JLPT no longer publishes item lists.

Result: cumulative vocab meets rough targets through N1 (`10,424 / 10,000`), but cumulative kanji does not (`889 / 2,000` for N1; `689 / 1,000` for N2). Grammar examples exist at every level: N1 `980`, N2 `764`, N3 `1000`, N4 `1000`, N5 `1180`.

Surprise: high. Vocab volume is broad, but upper kanji scope remains too thin for full N1/N2 coverage claims.

Update: content readiness split into vocab volume okay-ish, kanji scope weak, Vietnamese quality still weak.

Action: run D2.6 upper-kanji Han-Viet/Unihan spot check, then synthesize D2.

Time: 0.75h

EXPERIMENT E2.3 - Minna N3+ route gap

H tested: H2.3.1, H2.3.2, H2.3.3

Method: inspected local vocab route counts, checked official 3A Minna/Shin Kanzen pages, checked Vietnamese course/material pages, and attempted a Google Trends VN comparison.

Result: local Minna vocab route exists for N5/N4 only; N3-N1 vocab routes are `ShinKanzen` and `hajimete`. Official 3A pages show Minna Chukyu I/II and Vietnamese support. Vietnamese market/course pages map Minna to N5/N4 and Shin/Soumatome/Try/Mimikara to N3+.

Surprise: mild. Missing local Minna N3+ is not a hard blocker, but "Minna stops at N4" was too broad because the official series has Chukyu.

Update: beta can proceed with JLPT-level route labels if content quality passes, but product copy must not imply complete Minna continuity.

Action: queue Q2.5 scope comparison; add textbook-path question to beta onboarding later.

Time: 0.5h

## 2026-05-14T19:59:00+07:00

Active dimension: D8

Active question: Q8.4

Hypothesis being tested: H8.4.1-H8.4.4

Experiment status: measured

Commit hash: `26138b4c`

Surprise log: Moderate. A `firebase_crashlytics` BigQuery dataset exists, but the app has no Crashlytics/Sentry package, no global Flutter/platform error handlers, and no source-map or symbol upload path.

Belief update: production error monitoring is absent; Firebase Analytics product events cannot substitute for crash/error telemetry. Web beta needs Sentry-style error capture or an explicit accepted launch risk.

NS current value: real-user NS still unavailable; BigQuery identity/job path works, but GA4 export tables remain absent.

Next 6h plan: continue onboarding Phase 4, then D7.Q7.4 code-splitting feasibility. Live deploy remains blocked locally until `JPSTUDY_RECAPTCHA_SITE_KEY` is available.

EXPERIMENT E8.8 - Error monitoring design

H tested: H8.4.1-H8.4.4

Method: searched source, tests, workflow, and docs for Crashlytics/Sentry/error hooks/source-map upload; read `pubspec.yaml`, `main.dart`, analytics code, CI workflow, and official Firebase Crashlytics / Sentry Flutter docs.

Result: product analytics is present and consent-gated, but runtime error monitoring is not wired. No `firebase_crashlytics`, no `sentry_flutter`, no `FlutterError.onError`, no `PlatformDispatcher.instance.onError`, no `runZonedGuarded`, no non-fatal recorder, no source-map/symbol upload, and no first-crash verification step were found.

Surprise: moderate. Firebase-side Crashlytics dataset visibility does not imply app-side crash capture.

Update: choose Sentry for web beta first if vendor/secrets/privacy approval exists; defer Crashlytics to native Android/iOS distribution.

Action: wrote Q8.4 docs and updated the research index.

Time: 0.3h

## 2026-05-14T14:20:00+07:00

Active dimension: D1

Active question: Q1.4

Hypothesis being tested: H1.4.1-H1.4.3

Experiment status: partially unblocked

Commit hash: `e468d6c7`

Surprise log: Moderate. Service-account auth and BigQuery Job User access work, but the requested `firebase_sessions.sessions_*` wildcard currently has no matching tables, despite the dataset being present.

Belief update: D1 is unblocked at the identity/job layer, but real event-table availability still needs a table landing check after the export window.

NS current value: real-user NS still unavailable; GA4 dataset `analytics_536663906` is expected after first export table creation.

Next 6h plan: wait for `analytics_536663906.events_*`, then export GA4 rows with `tool/research/bigquery_runner.py` and run NS/funnel reports.

EXPERIMENT E1.10 - BigQuery service-account verification

H tested: real BigQuery direct query path.

Method: added a Python BigQuery runner and unit tests; used the matching local service-account key to mint an OAuth token via REST after local Python SDK install failed; ran BigQuery metadata/job checks.

Result: runner tests passed `4/4`. OAuth token mint succeeded. `SELECT 1 AS ok` BigQuery job succeeded with `totalRows=1`. Dataset listing succeeded. `firebase_crashlytics`, `firebase_messaging`, and `firebase_sessions` currently list `0` tables. `SELECT 1 AS ok FROM jpstudy-v2.firebase_sessions.sessions_* LIMIT 1` failed because no table matched the wildcard.

Surprise: moderate. Access is real, but table availability is not yet as expected.

Update: use direct BigQuery as primary path; keep GA4 Data API as backup only if export tables do not appear.

Action: updated Q1.4 docs and SQL; no real NS report yet.

Time: 0.4h

## 2026-05-14T14:22:00+07:00

Active dimension: D5

Active question: Q5.5

Hypothesis being tested: H5.5.1-H5.5.5

Experiment status: done

Commit hash: `e468d6c7`

Surprise log: Strong. Grammar ghost review looks like an active repair loop, but the practice screen does not update the `ghostReviewsDue` source that feeds the visible ghost list.

Belief update: vocab/kanji mistakes are a simple correct-streak queue; grammar ghosts are state-split and can remain stale.

NS current value: real-user NS still unavailable; BigQuery identity/job path verified, GA4 tables pending.

Next 6h plan: either implement Q5.5b ghost source-of-truth fix or continue D5.Q5.6 cross-skill prerequisites audit.

EXPERIMENT E5.5 - Mistake review and ghost algorithm audit

H tested: H5.5.1-H5.5.5

Method: inspected mistake schema/DAO/repository, vocab ghost provider/screen, grammar ghost providers/screens, grammar review recording, mistake bank UI, and weakness radar priority; ran focused mistake/ghost tests.

Result: focused tests passed `72/72`. `user_mistakes` stores count/recency/context, not a scheduler. New mistakes require two correct marks to clear. Grammar uses `ghostReviewsDue` for current providers but older `AttemptAnswer` rows for `markGrammarAsMastered()`. Ghost practice scoring does not update `ghostReviewsDue` or mistake rows.

Surprise: strong. The grammar ghost practice UI can complete without repairing the visible ghost.

Update: current grammar ghost remediation is not production-reliable.

Action: wrote Q5.5 docs; no behavior change yet.

Time: 0.5h

## 2026-05-14T14:21:00+07:00

Active dimension: D5

Active question: Q5.4

Hypothesis being tested: H5.4.1-H5.4.5

Experiment status: done

Commit hash: `e468d6c7`

Surprise log: Strong. Onboarding appears to offer goals, but only `writing` changes the immediate path; first-win and daily-plan logic are mostly generic level/due-count flows.

Belief update: onboarding is minimal setup, not a pedagogical fork engine.

NS current value: real-user NS still unavailable; BigQuery identity/job path is now verified but real GA4 tables are pending.

Next 6h plan: continue D5/D6 hardening while waiting for `analytics_536663906.events_*`; prioritize profile policy only after route/channel parity.

EXPERIMENT E5.4 - Onboarding fork logic audit

H tested: H5.4.1-H5.4.5

Method: inspected onboarding state, screen, persistence, analytics, home routing, continue action, and daily-plan code; searched for deadline/cramming/time budget/travel/group/news/accessibility fork concepts; ran focused onboarding/home tests.

Result: focused tests passed `43/43`. Persisted onboarding state is completed/level/goal only. First-win preview always uses vocab by level with N5 fallback. `StudyGoal.writing` redirects to kana foundations, but JLPT and reading land on generic home. Daily plan ignores `studyGoalProvider` and uses fixed new-content batches when review debt is low.

Surprise: strong. The selected goal is more telemetry/profile copy than downstream product behavior.

Update: do not claim persona-specific personalization from current onboarding.

Action: wrote Q5.4 docs; no behavior change yet.

Time: 0.4h

EXPERIMENT E2.4 - Cross-link coverage graph

H tested: H2.4.1, H2.4.2, H2.4.3

Method: added TDD-backed graph scanner and CLI report for vocab-kanji, kanji-example, and grammar-example links across `assets/data/content`.

Result: grammar example groups mostly resolve to grammar points (`976/980` N1, full match N2-N5). Same-level kanji coverage is shallow: N1 `780/6379`, N2 `504/2991`, N3 `188/1786`, N4 `393/1719`, N5 `549/1470` kanji-bearing vocab entries fully covered. N4/N5 source refs resolve `353/381` and `401/452`.

Surprise: high. Grammar linking is strong, but vocab-kanji prerequisite inference is much weaker than route counts imply.

Update: no prerequisite gating until cumulative kanji coverage and unresolved ref samples are measured.

Action: run Q2.5 scope comparison and later cumulative kanji graph.

Time: 0.75h

EXPERIMENT E1.7 - Minimum event set SM1 gap

H tested: H1.3.1, H1.3.2, H1.3.3

Method: read current analytics methods/call sites, mapped SM1-SM8 to telemetry or audit artifacts, added `onboarding_completed {level, goal}`, and verified the event contract with service tests.

Result: `AnalyticsService` emits `onboarding_completed`; `HomeScreen` calls it after onboarding preferences persist. Analyzer and scoped analytics/research tests pass.

Surprise: mild negative. HomeScreen widget integration test hit Flutter Windows native-assets/sqlite copy crash and timeout, so service-level verification is stronger than widget-level verification.

Update: SM1 is closer to measurable, but DebugView or real GA4 export is still required.

Action: queue DebugView/export verification and explicit first-SRS funnel report.

Time: 0.5h

EXPERIMENT E2.1 - Vietnamese content review status

H tested: H2.1.1, H2.1.2, H2.1.3

Method: added a TDD-backed structured JSON scanner for `assets/data/content`, then counted machine-origin, explicit open-review, and approval signals by JLPT level and dataset.

Result: current scan covers 781 JSON files and 23,444 content items. N1/N2 contain 7,453 machine-origin items; 1,886 explicit open-review items appear after including `manual-review-needed`; vocab has 5,273 machine-origin rows with no approval/open-review status.

Surprise: high. The March Vietnamese audits scanned 351 files, so they are stale for current launch readiness; Q2.2 sampling also revealed `manual-review-needed` as a second open-review tag family.

Update: D2 needs a review-status taxonomy/backfill before broad content editing or launch claims.

Action: define D2.2 status schema, then prioritize N5/N4 learner-critical paths plus N1/N2 open-review examples.

Time: 0.75h

EXPERIMENT E2.2 - Grammar explanation sample quality

H tested: H2.2.1, H2.2.2, H2.2.3

Method: deterministic SHA-256 sample, seed `jpstudy-d2-q2.2-v1`, 2 grammar explanations per JLPT level. Rated Vietnamese fluency, pedagogic clarity, accuracy vs local source fields, and learner-confusion flag.

Result: all 4 sampled N1/N2 `approved-by-user` grammar explanations scored clarity `2/5`; all would confuse a learner. N3-N5 averaged fluency `4.2/5`, clarity `3.5/5`, accuracy `4.2/5`, with 1 confusion flag. Overall sample: clarity `2.9/5`, confusion `5/10`.

Surprise: high. Approval metadata is not a safe proxy for learner-ready explanations; Q2.2 also surfaced `manual-review-needed`, increasing Q2.1 open-review count to `1,886`.

Update: D2 must audit/rewrite high-risk approved N1/N2 grammar, not just open-review examples.

Action: queue severity-aware grammar sampler and status taxonomy/backfill before broad edits.

Time: 0.75h

EXPERIMENT E1.9 - GA4 BigQuery export handoff

H tested: H1.4.1, H1.4.2, H1.4.3

Method: created a narrow GA4 BigQuery SQL export for NS and SM1 events, plus runbook docs for scoring the resulting JSON with existing report tools.

Result: `tool/research/ga4_ns_export.sql` exists. It was not executed because `gcloud`/`bq` are unavailable locally.

Surprise: none. This is an access blocker, not a code blocker.

Update: Q1.4 can proceed as soon as a real export sample is available; raw export path is ignored by git.

Action: obtain BigQuery access or a downloaded JSON export.

Time: 0.25h

## 2026-05-14T18:38:00+07:00

Active dimension: D7

Active question: Q7.1b

Hypothesis being tested: H7.1.3 follow-up / ablation

Experiment status: implemented startup grammar seed removal

Commit hash: `aa5a2c441301bc43f7770325b41f0f4a7dd02bd7`

Surprise log: Strong positive. Removing the app DB startup grammar seed dropped root grammar JSON to zero without breaking focused grammar tests.

Belief update: root does not need grammar seed at all. Requested-level seeding belongs at grammar/lesson/JLPT call sites, not app DB provider initialization.

NS current value: real-user NS still unavailable; BigQuery identity/job path works, but GA4 export tables remain absent.

Next 6h plan: turn the manual Playwright resource-count smoke into a checked-in gate, then continue D8 monitoring/live gates while waiting for GA4 export.

EXPERIMENT E7.5 - Remove app startup grammar seeder

H tested: H7.1.3 follow-up / ablation

Method: removed `databaseProvider` fire-and-forget `GrammarSeeder.seedGrammarData()`; kept on-demand `GrammarRepository.fetchPointsByLevel(level)` seeding. Rebuilt release web, served `build/web`, and remeasured PerformanceResourceTiming.

Result: focused grammar/on-demand tests passed `13/13`; analyzer passed. First-route resource count dropped `108 -> 69`; `jsonCount` dropped `52 -> 2`; `grammarResourceCount` dropped `50 -> 0`. FCP was `188 ms` on localhost.

Surprise: strong positive. The root route had no need to fetch grammar after startup seeding was removed.

Update: D7 root-load content prefetch is materially improved. Remaining work is automation plus live-host measurement.

Action: updated D7 docs and research index; no deploy attempted.

Time: 0.3h

## 2026-05-14T18:54:00+07:00

Active dimension: D7 / D8

Active question: Q7.1b / Q8.5

Hypothesis being tested: H7.1.3 follow-up, H8.5.3

Experiment status: implemented checked-in resource-count gate

Commit hash: `aa5a2c441301bc43f7770325b41f0f4a7dd02bd7`

Surprise log: Mild positive. A clean Playwright Chromium run reports an even lower root count than the manual MCP browser because it has no local extension noise.

Belief update: local root resource regression can now be guarded in CI; live-host proof remains a separate D8 release gate.

NS current value: real-user NS still unavailable; BigQuery identity/job path works, but GA4 export tables remain absent.

Next 6h plan: switch to live Firebase Hosting verification/deploy policy or D8.Q8.4 error monitoring; do not optimize root perf further until live metrics exist.

EXPERIMENT E7.6 - Checked-in web resource smoke

H tested: H7.1.3 follow-up and H8.5.3

Method: added `tool/research/web_resource_smoke.js`, Node unit tests, npm scripts, `playwright` devDependency, and GitHub Actions steps after web build. The script serves `build/web`, launches Chromium, reads PerformanceResourceTiming, and enforces max resource/json/grammar counts.

Result: RED unit test failed on missing module; GREEN unit passed `2/2`. `npm run test:web-resource-smoke` passed with `resourceCount=38`, `jsonCount=1`, `grammarResourceCount=0` under budgets `80/5/0`. CI now installs Node deps, installs Playwright Chromium, runs the unit test, and runs the smoke after the artifact budget.

Surprise: mild positive. Extension-free Chromium gives a cleaner root count than manual MCP Playwright, so the gate is stricter and less noisy.

Update: D7 local root resource-count budget is now reproducible. Remaining perf proof must be live-host/Lighthouse, not another manual local smoke.

Action: added tool/test/scripts/CI gate and updated D7/D8 docs.

Time: 0.6h

EXPERIMENT E1.8 - SM1 funnel report

H tested: H1.3.1

Method: wrote failing tests for a funnel evaluator and CLI report, then scored the existing normalized fixture.

Result: existing fixture reports observed users 2, opened users 0, onboarded users 0, first SRS users 2.

Surprise: mild. The current NS fixture validates gates but is insufficient for SM1 because it predates `onboarding_completed` and has no open/session events.

Update: SM1 now has a one-command report, but old/exportless data cannot populate it.

Action: run `tool/research/funnel_report.dart` on the first real GA4 export sample.

Time: 0.5h

EXPERIMENT E2.6 - Kanji Han-Viet / Unihan spot check

H tested: H2.6.1, H2.6.2, H2.6.3

Method: added a TDD-backed deterministic sampler for N3/N2/N1 kanji, parsed local Unihan `kVietnamese`, and compared a 50-row seed sample with local `labels.hanViet`.

Result: sample size 50 from 603 candidate rows. Exact matches: 22. Missing local Han-Viet: 23. Missing Unihan `kVietnamese`: 4. Mismatches: 1 (`行`, local `Hành`, Unihan `hàng`).

Surprise: high. Existing Unihan credit/source trail does not mean learner-facing Han-Viet fields are complete.

Update: upper Han-Viet metadata is useful only after a completeness audit/backfill and manual review of ambiguous readings.

Action: add full-corpus upper-kanji completeness audit and backfill plan before any upper-kanji launch claim.

Time: 0.5h

EXPERIMENT E3.1-E3.3 - Vietnamese app-language, hardcoded copy, and mojibake audit

H tested: H3.1.1-H3.1.3, H3.2.1-H3.2.3, H3.3.1-H3.3.3

Method: added a TDD-backed audit scanner for `app_language.dart`, hardcoded Vietnamese in `lib/`, mojibake markers across `lib/`, content JSON, and docs, plus decode-error capture for non-UTF8 markdown.

Result: `app_language.dart` has `680` localized returns per locale, `0` empty returns, and `0` TODO/draft returns. Non-`app_language.dart` Dart has `1,893` Vietnamese lines after excluding research helper code. Mojibake scan found `7` Dart-source hits, which were fixed and guarded to `0`; content JSON has `0` hits; the `3` UTF-16 LE docs files were converted to UTF-8, so current decode errors are `0`.

Surprise: high. App-language coverage is balanced, but the actual Vietnamese editorial surface is much larger than one file.

Update: D3 needs copy ownership classification and terminology glossary before broad wording fixes.

Action: classify top hardcoded-copy files and keep the UTF-8 decode gate in the D3 real-data test.

Time: 0.75h

EXPERIMENT E3.2B - Hardcoded Vietnamese ownership classification

H tested: follow-up to H3.2.2 and H3.2.3

Method: inspected the top ten files from the D3 hardcoded-copy report and classified each as domain data, feature copy module, or local UI/helper copy.

Result: top files split into domain data (`radical_item.dart`), existing feature copy (`kanji_copy.dart`, `vocab_copy.dart`), and local UI/helper copy that should be extracted (`custom_decks_screen.dart`, `minna_lesson_catalog_screen.dart`, provider copy, grammar/premium/community screens). Wrote glossary seed for terms like `review`, `quiz`, `deck`, `lane`, `kanji`, and `SRS`.

Surprise: mild. The problem is not one architecture failure; it is several ownership styles mixed without a glossary.

Update: use feature copy modules as intermediate architecture; do not jump straight to ARB migration.

Action: apply glossary to highest-risk learner-facing strings, starting with vocab/kanji/custom-deck copy.

Time: 0.5h

EXPERIMENT E3.2C - First glossary replacement patch

H tested: D3 glossary seed can reduce high-risk mixed English/Vietnamese UI copy with small, testable edits.

Method: added focused tests for vocab copy and selected `AppLanguage` strings, verified RED, then replaced `Review`, `lane`, `Catalog`, `Preview`, `deck`, and `Kanji` casing in the tested UI-copy surface.

Result: focused tests pass for `VocabCopy` and selected `AppLanguage` Vietnamese labels.

Surprise: none. The copy API made these small replacements cheap.

Update: glossary-first edits are viable for high-traffic labels before a larger i18n migration.

Action: continue with suspicious custom-deck strings and classify whether they need tests or extraction first.

Time: 0.25h

EXPERIMENT E3.4 - Vietnamese typography fixed-seed sample

H tested: H3.4.1-H3.4.5

Method: added a TDD-backed typography sampler over `app_language.dart` Vietnamese returns and hardcoded Vietnamese Dart strings in `lib/`, then ran a fixed seed (`20260514`) sample of `100`.

Result: `2,196` candidates, `100` sampled, average score `4.84/5`, `92/100` clean strings, `8` raw-English-term warnings, `0` mojibake/punctuation/tone-variant warnings.

Surprise: mild. The typography surface is cleaner than expected after encoding cleanup, but mixed English product terms remain concentrated outside `app_language.dart`.

Update: Q3.4 is a triage gate, not human editorial approval. Next D3 risk is architecture/ownership: whether ARB migration is worth the rewrite cost.

Action: continue to Q3.5 ARB recommendation with current evidence.

Time: 0.5h

EXPERIMENT E3.5 - ARB migration cost recommendation

H tested: H3.5.1-H3.5.4

Method: inspected existing Flutter localization hooks, counted `AppLanguage`/`appLanguageProvider` call sites, checked for generated localization artifacts, and compared migration cost against D3.Q3.1-Q3.4 findings.

Result: `app_language.dart` is `7,561` lines with `680` returns per locale. `AppLanguage` appears in `140` lib files, `appLanguageProvider` in `111`, and `AppLanguage.en/vi/ja` references occur `5,219` times. There is `flutter_localizations` plus `MaterialApp.locale`, but `0` `.arb` files and no `l10n.yaml`.

Surprise: mild. Flutter localization plumbing is partly present, but the app-language enum is also domain/theme/provider state, not just generated UI copy.

Update: full ARB migration is a post-beta architecture project. Pre-beta D3 should continue with feature-copy modules, glossary tests, and Q3.6 plural/ICU audit.

Action: run Q3.6 plural/ICU audit next.

Time: 0.5h

EXPERIMENT E3.6 - Plural and ICU risk audit

H tested: H3.6.1-H3.6.4

Method: grep-searched actual Dart source for ICU/plural APIs, manual singular/plural guards, interpolated English count strings, and central `AppLanguage` count-label helpers.

Result: `0` `Intl.plural` / plural API matches, `5` relevant manual singular guards, `41` raw English count-plural strings across `18` files, and `6,942` interpolated strings in `lib/`.

Surprise: mild. The issue is real, but it is narrow enough for helper-based fixes; it does not justify full ARB migration before beta.

Update: D3 is now measured across Q3.1-Q3.6. Remaining D3 work is synthesis plus targeted glossary/plural fixes.

Action: write D3 synthesis, then move to D4 persona UAT or patch central plural helpers if chosen as a pre-UAT polish item.

Time: 0.4h

EXPERIMENT E3.6B - Central English plural helper patch

H tested: Q3.6 central count helpers can remove the highest-risk `1 items` class without ARB migration.

Method: added RED tests for singular and plural English `AppLanguage` count helpers, patched only the tested helper methods, then reran the plural-risk grep.

Result: tested helpers now return singular English for count `1` and plural for count `2`. Raw English count-plural grep dropped from `41` to `31` matches.

Surprise: none.

Update: ARB still not needed pre-beta; remaining plural risk is feature-local copy.

Action: move to D4 persona UAT; leave remaining feature-local plural strings for D5/D6 polish unless UAT exposes them.

Time: 0.25h

## 2026-05-14T10:19:17+07:00

Active dimension: D4

Active question: Q4.P2

Hypothesis being tested: H4.P2.1-H4.P2.5

Experiment status: done

Commit hash: `8387e444`

Surprise log: Strong. Correct JSON-encoded prefs only work on root start; direct `/#/grammar` cold start falls back to N5 because app init is watched in `HomeScreen`, not globally. Live `/exam-center` also differs from local code and mobile reading CTA can be hit by bottom nav.

Belief update: P2 readiness is route/entry-path fragile, not just content-fragile. Mixed-level beta cannot rely on users entering through `/`.

NS current value: real-user NS still unavailable; synthetic and telemetry docs exist only.

Next 6h plan: finish P2 docs, then either fix global init/mobile CTA or continue P3 UAT depending on highest expected information gain.

EXPERIMENT E4.P2 - Anh Tuấn N3 persona UAT

H tested: H4.P2.1-H4.P2.5

Method: seeded SharedPreferences web state for N3/JLPT/VI, tested root and deep-link route starts on desktop/mobile via Playwright, and avoided quiz/review submission.

Result: root N3 home works with `~14 phút`; direct grammar hard reload shows N5; JLPT coach/reading N3 works after root init; live exam route is legacy/inconsistent; mobile reading has mojibake chips and CTA occlusion; vocab N3 reports `Hajimete N3 (0 mục từ)`.

Surprise: strong. Level persistence is not global across route entry, and live deploy route behavior does not match local route code.

Update: D4 risk is now high enough to prioritize route bootstrap and mobile CTA tests before treating remaining P2 issues as copy polish.

Action: local TDD patch fixed study-goal copy and JLPT reading meta chips; D4.P2 docs written.

Time: 1.5h

EXPERIMENT E4.P2B - Direct-route app init patch

H tested: H4.P2.1 root cause is app init being triggered only by `HomeScreen`.

Method: added a RED widget test for direct `/grammar` route context with persisted `n3`; moved `appInitProvider` watch to app bootstrap; reran route/home/JLPT focused tests.

Result: new `test/app/app_init_deep_link_test.dart` passes and verifies `studyLevelProvider == StudyLevel.n3` in a direct grammar route context. Focused regression set: `19/19` passed.

Surprise: mild. The provider state can be verified independently of full static-router text rendering; existing route smoke tests still pass.

Update: local code now addresses the highest P2 route-entry blocker; live deploy remains unverified.

Action: next P2 blocker is mobile CTA occlusion or live `/exam-center` parity.

Time: 0.5h

## 2026-05-14T11:32:27+07:00

Active dimension: D4

Active question: Q4.P2

Hypothesis being tested: H4.P2.4

Experiment status: done

Commit hash: `8387e444`

Surprise log: Mild. The CTA issue was not only bottom padding; compact mobile reading still delayed the primary action behind hero/notice/card preview, and delayed card animations leaked timers in widget tests.

Belief update: For commute-sized screens, the reading route must surface the start action before secondary explanation; animation flourish is lower priority than touch safety.

NS current value: real-user NS still unavailable; synthetic and telemetry docs exist only.

Next 6h plan: run final focused regression/analyze checks, then choose live `/exam-center` parity or P3 UAT.

EXPERIMENT E4.P2C - Mobile reading CTA regression patch

H tested: H4.P2.4

Method: added a RED `390x844` widget layout test with a 102px bottom-nav overlay, then compacted the mobile reading header, removed the compact picker notice, moved card CTA above preview, and removed delayed card-list animations.

Result: combined JLPT reading tests passed: `5/5`. The new regression asserts the first reading CTA clears the bottom-nav area by `8px`.

Surprise: mild. The previous delayed card animations created pending widget-test timers once compact layout made the first card visible.

Update: local P2 mobile reading CTA blocker has a regression test and mitigation; live deploy remains pre-fix.

Action: include mobile layout test in focused regression; later verify on deployed mobile viewport.

Time: 0.5h

EXPERIMENT E4.P2D - Live exam-center parity probe

H tested: H4.P2.3

Method: cleared live origin storage, seeded JSON-encoded N3/JLPT/VI prefs, hard-loaded `https://jpstudy-v2.web.app/#/exam-center`, captured screenshot evidence, then inspected deployed `main.dart.js` route builders around `/exam-center`.

Result: live still renders `Đề thi thử JLPT N3` with `Bài học này chưa có từ vựng.` Deployed JS maps `/exam-center` and `/practice/mock-exam` to the same widget constructor (`A.Ex`), while local `exam_routes.dart` maps `/exam-center` to `ExamCenterHubScreen`.

Surprise: mild. The route bug is not in current local code; the live channel is stale or pointing at an older build.

Update: P2 live exam blocker needs deploy/channel verification, not another local route patch.

Action: request/perform explicit hosting deploy after final local verification; then rerun live `/exam-center` screenshot.

Time: 0.25h

## 2026-05-14T11:58:00+07:00

Active dimension: D4

Active question: Q4.P3

Hypothesis being tested: H4.P3.1-H4.P3.5

Experiment status: done

Commit hash: `e468d6c7`

Surprise log: Strong. Live root can show N2, but direct grammar/vocab/kanji/coach/reading hard loads still fall to N5, and `/exam-center` remains a stale/empty chooser.

Belief update: P3 readiness is blocked less by laptop layout and more by deployed-channel trust plus missing cramming intensity. A 3-hour/day N2 student needs a long-session JLPT command center, not only a short habit plan.

NS current value: real-user NS still unavailable; synthetic and telemetry docs exist only.

Next 6h plan: verify hosting channel/deploy parity before more route work, then decide whether D4 should continue P4/P5 UAT or build a cramming-mode slice.

EXPERIMENT E4.P3 - Mai N2 persona UAT

H tested: H4.P3.1-H4.P3.5

Method: seeded JSON-encoded SharedPreferences web state for N2/JLPT/VI, tested root and direct study/exam/social routes at `1366x768` with Playwright, and saved screenshots to `tests/uat-p3-2026-05-14/`.

Result: root N2 home works and shows `~14 phút`; direct grammar/vocab/kanji/coach/reading routes show N5; exam center shows only `Thi thử` / `Chọn cấp JLPT`; leaderboard has `Chia sẻ snapshot`; community invite/room features are roadmap states.

Surprise: strong. P2's direct-route/live parity defect generalizes to N2 and blocks a more advanced persona.

Update: No new local patch was made. Current local app init already has a regression test; P3 exposes live-channel drift plus product-scope gaps around cramming intensity and group study.

Action: document P3 as beta FAIL; make deploy/channel verification the next prerequisite before re-testing direct-route readiness.

Time: 0.75h

## 2026-05-14T12:35:00+07:00

Active dimension: D4

Active question: Q4.P4

Hypothesis being tested: H4.P4.1-H4.P4.5

Experiment status: done

Commit hash: `e468d6c7`

Surprise log: Mild-to-strong. Tablet root at 125% zoom is readable and N4-aware, but there is no travel goal, no visible font-size setting, and direct study routes still fall to N5.

Belief update: P4 risk is two-layered: production channel parity for N4 direct routes, then product positioning for older non-exam learners.

NS current value: real-user NS still unavailable; synthetic and telemetry docs exist only.

Next 6h plan: either run P5 Sora N1 UAT to complete D4 persona coverage, or pause for explicit production deploy/channel verification because P2-P4 all hit the same live direct-route blocker.

EXPERIMENT E4.P4 - Bác Hùng N4 persona UAT

H tested: H4.P4.1-H4.P4.5

Method: seeded JSON-encoded SharedPreferences web state for N4/reading/VI, used `834x1194` tablet viewport with approximately 125% browser zoom, slow-tapped the root plan, and probed direct study/community routes with Playwright.

Result: root N4 is readable; slow tap opens an N4 `Học` plan with `Bắt đầu Minna No Nihongo 26`, `~15p`, and `Mở immersion`; direct kanji/vocab/grammar/reading/study-hub fall to N5; community is readable but has no font-size setting.

Surprise: mild-to-strong. P4 did not primarily fail on visual readability; it failed on route trust and missing non-exam goal vocabulary.

Update: P4 is beta FAIL until channel parity is fixed and travel/fun/accessibility scope is decided.

Action: document P4, keep screenshots in `tests/uat-p4-2026-05-14/`, and queue P5 or deploy verification next.

Time: 0.75h

## 2026-05-14T13:05:00+07:00

Active dimension: D4

Active question: Q4.P5

Hypothesis being tested: H4.P5.1-H4.P5.5

Experiment status: done

Commit hash: `e468d6c7`

Surprise log: Strong. N1 immersion content exists and looks useful after root init, but direct advanced routes still fall to N5 and study hub does not surface N1/news reading.

Belief update: Advanced-reader readiness is not blocked by total N1 reading absence; it is blocked by routing/discovery and by the missing news/current-world promise.

NS current value: real-user NS still unavailable; synthetic and telemetry docs exist only.

Next 6h plan: write D4 persona synthesis, then decide whether the next action is deploy/channel verification, N1 immersion promotion, or moving to D5/D6.

EXPERIMENT E4.P5 - Sora N1 persona UAT

H tested: H4.P5.1-H4.P5.5

Method: seeded JSON-encoded SharedPreferences web state for N1/reading/VI, tested root, direct advanced routes, initialized hash navigation to immersion/study hub, and opened one N1 immersion deck.

Result: root N1 works; direct immersion/reading/study hub/vocab/grammar fall to N5; initialized immersion shows `Track N1`, 25 N1 decks, and an advanced annotated passage; study hub advanced filter shows N3 grammar/listening only; no news/current route surfaced.

Surprise: strong. There is a high-quality N1 island inside immersion, but the main advanced discovery paths do not lead to it reliably.

Update: P5 is beta FAIL until direct route parity and advanced-reading discovery are fixed; news scope needs explicit product decision.

Action: document P5 and synthesize D4 cross-persona blockers.

Time: 0.75h

## 2026-05-14T13:25:00+07:00

Active dimension: D4

Active question: Q4.global

Hypothesis being tested: cross-persona readiness for 100 mixed N5-N1 Vietnamese learners

Experiment status: done

Commit hash: `e468d6c7`

Surprise log: Strong. P1 N5 core smoke is viable, but P2-P5 all fail broad beta readiness. The repeated direct-route N5 fallback dominates all persona-specific issues.

Belief update: D4's top leverage is channel parity and route-level level trust, not another local per-screen polish pass. Persona scope decisions are second: cramming, travel/fun, group study, and N1 news are not all supportable without product work.

NS current value: real-user NS still unavailable; synthetic and telemetry docs exist only.

Next 6h plan: ask/perform explicit deploy-channel verification, then rerun route matrix; otherwise move to D5/D6 only with caveat that live route trust remains unresolved.

EXPERIMENT E4.S - Cross-persona synthesis

H tested: P1-P5 persona readiness.

Method: synthesized Linh N5 UAT notes plus P2-P5 hypothesis/experiment/raw/analysis docs.

Result: P1 N5 core modules passed smoke after fixes. P2-P5 failed: live direct routes fall to N5, `/exam-center` is stale/empty, planning lacks intensity, study goals omit travel/fun, social/group is roadmap, N1 immersion exists but discovery/news are weak.

Surprise: strong. The same route/channel defect explains more beta risk than any single content screen.

Update: JpStudy-v2 is not ready for 100 mixed N5-N1 beta learners. A narrow controlled pilot may be possible after live channel parity and route matrix pass.

Action: wrote `docs/research/D4-persona-synthesis.md`.

Time: 0.4h

EXPERIMENT E4.C - Firebase hosting channel parity check

H tested: live route failures may be stale-channel evidence rather than current local source behavior.

Method: read `.firebaserc`, `firebase.json`, local `HEAD`, and Firebase Hosting channel metadata using local `firebase-tools` via `npm exec`; no deploy command was run.

Result: `.firebaserc` default project is `jpstudy-v2`; hosting targets include `jpstudy-v2` and `jpstudy`. `jpstudy-v2` live channel last release is `2026-05-13 01:32:40`; `jpstudy` live is `2026-05-13 21:34:45`; current `HEAD` is `e468d6c7` from `2026-05-14T11:52:56+07:00`. Global `firebase` is not on PATH; local `firebase-tools` is available via `npm exec`. `hosting:sites:list` also emitted a stale-credentials warning.

Surprise: mild. This confirms the D4 live failures are being measured against a hosting release older than current local source.

Update: channel parity is not optional; rerunning persona direct routes before deploy will mostly re-measure a stale artifact.

Action: no deploy attempted; needs explicit production deploy/channel decision.

Time: 0.2h

## 2026-05-14T13:40:00+07:00

Active dimension: D5

Active question: Q5.1

Hypothesis being tested: H5.1.1-H5.1.5

Experiment status: done

Commit hash: `e468d6c7`

Surprise log: Strong. Existing FSRS tests pass, but the implementation is a legacy `17`-parameter scheduler without FSRS learning/relearning state. New-card `Good` is scheduled `3,456` minutes out locally versus `10` minutes in the reference scheduler.

Belief update: SRS risk is not just calibration for Japanese vocabulary; it is algorithm/state mismatch before calibration can be trusted.

NS current value: real-user NS still unavailable; synthetic and telemetry docs exist only.

Next 6h plan: decide whether to implement Q5.1b scheduler state/step migration before continuing Q5.2 streak logic, because SRS correctness directly affects the North Star review/retention metric.

EXPERIMENT E5.1 - FSRS correctness audit

H tested: H5.1.1-H5.1.5

Method: compared `FsrsService`, SRS persistence, and existing tests against current Open Spaced Repetition FSRS-6 sources; added a deterministic local audit report and tests.

Result: local parameter count `17` vs FSRS-6 `21`; no persisted learning state or step; first-review intervals local/reference were `Again 576/1`, `Hard 864/5.5`, `Good 3456/10`, `Easy 8352/11520` minutes. Focused tests passed `73/73`.

Surprise: strong. The most learner-critical path is new-card learning, and it diverges by hours/days rather than minor formula drift.

Update: current scheduler should be treated as FSRS-like legacy behavior, not FSRS-6.

Action: wrote Q5.1 docs and added `tool/research/fsrs_correctness_report.dart`.

Time: 0.7h

## 2026-05-14T18:29:00+07:00

Active dimension: D7

Active question: Q7.1b

Hypothesis being tested: H7.1.3 follow-up

Experiment status: measured post-fix browser resource count

Commit hash: `aa5a2c441301bc43f7770325b41f0f4a7dd02bd7`

Surprise log: Mild positive. Resource count dropped more than half after level-scoping grammar seeders, but the first route still fetches the full active N5 grammar bank.

Belief update: all-level grammar eager loading was the largest measured startup request issue, but route-minimal grammar loading is still not done.

NS current value: real-user NS still unavailable; BigQuery identity/job path works, but GA4 export tables remain absent.

Next 6h plan: add checked-in browser resource-count tooling/budget, then continue D8 monitoring/live gates while waiting for `analytics_536663906`.

EXPERIMENT E7.4 - Post-fix browser resource-count smoke

H tested: H7.1.3 follow-up

Method: rebuilt release web with the App Check placeholder define, served `build/web` locally on `127.0.0.1:8099`, waited 5 seconds in Playwright, and read PerformanceResourceTiming entries.

Result: first-route `resourceCount` dropped `250 -> 108`; `jsonCount` was `52`; `grammarResourceCount` was `50`, all N5-only grammar/example files. FCP was `264 ms` on localhost. Firebase analytics/installations still produced localhost referrer `403` errors.

Surprise: mild positive. The lazy seed patch removed broad cross-level grammar fetches, but active-level grammar still loads eagerly.

Update: D7 now has proof of improvement, not just code-level reasoning. Remaining D7 work is making this smoke reproducible and budgeting the `108` baseline lower over time.

Action: updated D7 docs and research index; no deploy attempted.

Time: 0.25h

## 2026-05-14T14:25:00+07:00

Active dimension: D5

Active question: Q5.2

Hypothesis being tested: H5.2.1-H5.2.5

Experiment status: done

Commit hash: `e468d6c7`

Surprise log: Strong. Streak looked like a simple daily counter, but the global write paths are uneven: vocab review updates `user_progress`, while grammar review maintains only grammar-local streak.

Belief update: streak cannot be used as a clean retention proxy until all learner-facing study modes share one policy and one recorder.

NS current value: real-user NS still unavailable; synthetic and telemetry docs exist only.

Next 6h plan: either implement Q5.2b `StreakPolicy`/unique-day/upsert tests or continue D5.Q5.3 XP formula with this caveat.

EXPERIMENT E5.2 - Streak rules audit

H tested: H5.2.1-H5.2.5

Method: inspected streak/day-key/progress code, compared with Duolingo and Anki conventions, searched for freeze/grace/timezone concepts, and ran focused streak-adjacent tests.

Result: tests passed `52/52`. No streak freeze/grace/repair/timezone policy matches. Streak uses local `DateTime.now()` and `DateTime(year, month, day)`, carries yesterday's streak today, resets after a gap older than yesterday, has no `user_progress.day` unique key, and grammar SRS does not write global `user_progress`.

Surprise: strong. The unfairness is cross-skill write coverage, not just whether the app should add a streak freeze.

Update: current streak is a gamification display, not a trustworthy 30-day retention signal.

Action: wrote Q5.2 docs; no behavior change yet.

Time: 0.6h

## 2026-05-14T16:15:00+07:00

Active dimension: D6

Active question: Q6.1

Hypothesis being tested: H6.1.1-H6.1.5

Experiment status: done

Commit hash: `e468d6c7`

Surprise log: Mild. Card inconsistency is real, but much of it is an implicit taxonomy rather than random drift.

Belief update: the next UI step should document surface families before refactoring visuals.

NS current value: real-user NS still unavailable; GA4 export table still pending.

Next 6h plan: continue D6.Q6.2 empty-state audit or switch to D7 lighthouse baseline if live-channel parity remains the highest risk.

EXPERIMENT E6.1 - Card pattern audit

H tested: H6.1.1-H6.1.5

Method: read shared card primitives/theme tokens, grepped feature modules for card/surface patterns, and sampled 20 surfaces across home, vocab, kanji, grammar, exam, test, and learn.

Result: at least `10/20` sampled surfaces are custom or non-`AppSectionCard`. Radius values include `10`, `12`, `16`, `18`, `20`, `22`, `24`, and `28`. CTA placement uses bottom button wrap, header action, trailing arrow row, full-card tap, and no-CTA progress card.

Surprise: mild. Home and practice divergence is often intentional, but undocumented.

Update: use a three-family taxonomy: hub/info, dense home panel, active practice/task.

Action: wrote Q6.1 docs; no UI refactor yet.

Time: 0.4h

## 2026-05-14T15:10:00+07:00

Active dimension: D5

Active question: Q5.3

Hypothesis being tested: H5.3.1-H5.3.5

Experiment status: done

Commit hash: `e468d6c7`

Surprise log: Strong. XP looked like one account currency, but visible `+XP` paths are split between global `user_progress` writes, session-local storage, and display-only bonuses.

Belief update: XP can motivate locally, but `todayXp` and level cannot be used as normalized learning-effort metrics until reward sources have one policy.

NS current value: real-user NS still unavailable; synthetic and telemetry docs exist only.

Next 6h plan: either implement Q5.3b `XpPolicy`/visible-XP write tests, or continue D5.Q5.4 onboarding fork audit with caveat that XP is not metric-grade.

EXPERIMENT E5.3 - XP and level formula audit

H tested: H5.3.1-H5.3.5

Method: inspected XP generation/write paths across vocab SRS, Learn, tests, flashcards, games, weekly challenges, achievements, and level calculation; searched for cap/burnout terms; ran focused XP/level tests.

Result: focused tests passed `199/199`. Level curve is deterministic and tested, but top comments conflict with the increasing-gap ladder. No daily XP cap, burnout guard, diminishing returns, or throttle policy was found. Learn and flashcard summaries can display session XP without a discovered global `user_progress` write, while tests/games/challenges write account XP.

Surprise: strong. The inconsistency is not formula math; it is currency semantics.

Update: current XP is a partially populated gamification counter, not a trustworthy cross-module study-effort metric.

Action: wrote Q5.3 docs; no behavior change yet.

Time: 0.5h

## 2026-05-14T15:45:00+07:00

Active dimension: D5

Active question: Q5.6

Hypothesis being tested: H5.6.1-H5.6.5

Experiment status: done

Commit hash: `e468d6c7`

Surprise log: Strong. Cumulative lower-level kanji improves same-level coverage, but still fully covers only about one third of kanji-bearing vocab entries by level.

Belief update: cross-skill prerequisites should be advisory, not hard gates, until dependency extraction, availability labels, and telemetry exist.

NS current value: real-user NS still unavailable; BigQuery auth is ready but GA4 event export is still waiting on `analytics_536663906.events_*`.

Next 6h plan: continue D6 unless GA4 export tables appear first; if staying in D5, implement Q5.6b advisory prerequisite cards instead of route locks.

EXPERIMENT E5.6 - Cross-skill prerequisite audit

H tested: H5.6.1-H5.6.5

Method: added TDD-backed cumulative kanji coverage to the content link graph, ran the graph on real content assets, searched app code for prerequisite/gating/scoped-practice paths, and ran focused scoped kanji practice tests.

Result: focused tests passed `28/28`. Cumulative fully covered vocab entries are N1 `2483/6379`, N2 `1098/2991`, N3 `545/1786`, N4 `672/1719`, and N5 `549/1470`. `KanjiPracticeArgs.kanjiIds` supports scoped practice, but no discovered code maps weak vocab/grammar items to missing prerequisites.

Surprise: strong. The missing piece is not only UI routing; the graph does not support confident hard gating.

Update: prerequisite logic should start as "review these kanji first" suggestions with acceptance/recovery telemetry.

Action: wrote Q5.6 docs and extended `content_link_graph_report` with cumulative coverage.

Time: 0.6h

## 2026-05-14T16:55:00+07:00

Active dimension: D6

Active question: Q6.2

Hypothesis being tested: H6.2.1-H6.2.5

Experiment status: done

Commit hash: `e468d6c7`

Surprise log: Mild. A reusable `EmptyStateWidget` exists but is unused; the more active shared pattern is `ErrorStateWidget`.

Belief update: empty-state consistency needs policy first, not a broad replacement pass.

NS current value: real-user NS still unavailable; GA4 export table still pending.

Next 6h plan: continue D6.Q6.3 loading-state audit, or switch to D7 Lighthouse baseline if a browser/perf pass becomes higher leverage.

EXPERIMENT E6.2 - Empty-state audit

H tested: H6.2.1-H6.2.5

Method: grepped feature/core code for empty/load/error states, inspected reusable widgets and 20 sampled empty states, and ran focused empty-state widget tests.

Result: focused tests passed `92/92`. `EmptyStateWidget` has zero feature usages. At least `11/20` sampled states have no visible CTA; several optional home/me widgets use `SizedBox.shrink()` for empty/loading/error states.

Surprise: mild. Primary flows generally avoid crashes, but optional dashboard surfaces can hide missing/error state.

Update: define visible primary empty states versus optional collapsible panels.

Action: wrote Q6.2 docs; no UI refactor yet.

Time: 0.5h

## 2026-05-14T15:16:00+07:00

Active dimension: D6

Active question: Q6.3

Hypothesis being tested: H6.3.1-H6.3.5

Experiment status: done

Commit hash: `e468d6c7`

Surprise log: Mild. The app does have strong localized loading examples in Kanji Hub, JLPT, and Progress Coach, but they are exceptions rather than a shared policy.

Belief update: loading-state work should not start with a generic shimmer system. Start with route severity: primary blocking routes need visible localized loaders, optional dashboard panels need compact loading/error behavior.

NS current value: real-user NS still unavailable; BigQuery auth is ready, and GA4 export is still waiting on `analytics_536663906.events_*`.

Next 6h plan: continue D6.Q6.4 error-state audit, or switch to D7 Lighthouse/baseline if browser performance becomes higher leverage.

EXPERIMENT E6.3 - Loading-state audit

H tested: H6.3.1-H6.3.5

Method: grepped feature/core code for circular/linear indicators, skeleton/shimmer patterns, hidden loading branches, and loading copy; sampled primary routes, practice flows, dashboard panels, and newer localized loading cards.

Result: `CircularProgressIndicator` appears `69` times across `53` files; `LinearProgressIndicator` appears `32` times across `26` files; skeleton/shimmer search found only the progress coach skeleton; six home/me loading branches collapse with `SizedBox.shrink()`.

Surprise: mild. Good patterns exist, but they have not become the default loading language.

Update: define loading policy by surface criticality, not by one visual component.

Action: wrote Q6.3 docs; no UI refactor yet.

Time: 0.4h

## 2026-05-14T15:26:00+07:00

Active dimension: D6

Active question: Q6.4

Hypothesis being tested: H6.4.1-H6.4.5

Experiment status: done

Commit hash: `e468d6c7`

Surprise log: Mild. Error handling is stronger than loading handling because `ErrorStateWidget` and feature-specific retry cards already exist, but raw exception text still reaches several learner-facing routes.

Belief update: error-state cleanup should use severity rules, not a universal widget pass.

NS current value: real-user NS still unavailable; BigQuery auth is ready, and GA4 export is still waiting on `analytics_536663906.events_*`.

Next 6h plan: continue D6 accessibility/navigation audit or switch to D7 Lighthouse baseline while waiting for GA4 export tables.

EXPERIMENT E6.4 - Error-state audit

H tested: H6.4.1-H6.4.5

Method: grepped feature/core code for `ErrorStateWidget`, async error branches, retry labels, provider invalidation, raw exception rendering, hidden errors, snackbars, dialogs, and debug output; sampled primary routes, optional panels, and action failures.

Result: `ErrorStateWidget` has `13` feature usages after excluding self/test. Broad error/retry grep found `269` matches in `69` files. At least eight sampled error branches collapse with `SizedBox.shrink()`. Foundations, Grammar, and Recall Sprint can show raw exception text.

Surprise: mild. Good recovery patterns exist locally, especially Kanji Hub, Vocab Catalog, Search, Minna Catalog, and Immersion Home.

Update: primary route/practice errors need friendly persistent retry; action errors can stay snackbar-based; optional panels need compact error/telemetry instead of silent failure.

Action: wrote Q6.4 docs; no UI refactor yet.

Time: 0.4h

## 2026-05-14T15:38:00+07:00

Active dimension: D6

Active question: Q6.5

Hypothesis being tested: H6.5.1-H6.5.5

Experiment status: done

Commit hash: `e468d6c7`

Surprise log: Mild. Navigation contrast passed, but small semantic status colors are weaker than expected, especially warning.

Belief update: WCAG risk is token-level small text, not the main reading surface.

NS current value: real-user NS still unavailable; BigQuery auth is ready, and GA4 export is still waiting on `analytics_536663906.events_*`.

Next 6h plan: either implement Q6.5b contrast token fixes, continue D6.Q6.6 touch targets, or switch to D7 Lighthouse if performance baseline becomes higher leverage.

EXPERIMENT E6.5 - Contrast audit

H tested: H6.5.1-H6.5.5

Method: read actual theme/navigation/shared component code, grepped muted/disabled/hint/status usages, then ran manual WCAG contrast math with alpha blending for sampled token pairs.

Result: sidebar inactive labels pass at `6.35`-`6.43:1`; mobile nav inactive passes at `4.97:1`; body text passes at `6.56:1`. Input hints fail at `2.57:1`; `ink 0.45` helper text is `2.79:1`; `AppStatusChip.warning` is `2.55:1`; semantic small text is mostly large-only on light surfaces.

Surprise: mild. The app is not globally low-contrast; the issue is concentrated in muted helper/status tokens.

Update: fix contrast by token policy first: hint text, `AppStatusChip`, and small semantic chip foregrounds.

Action: wrote Q6.5 docs and `D6-contrast-audit.md`; no UI refactor yet.

Time: 0.5h

## 2026-05-14T15:46:00+07:00

Active dimension: D6

Active question: Q6.6

Hypothesis being tested: H6.6.1-H6.6.5

Experiment status: done

Commit hash: `e468d6c7`

Surprise log: Moderate. Main nav and quiz controls are safer than expected, but compact overrides show up across more secondary controls than the first discover-panel clue suggested.

Belief update: touch safety is not a full UI redesign problem; it is a min-hitbox policy problem for dense/inline controls.

NS current value: real-user NS still unavailable; BigQuery auth is ready, and GA4 export is still waiting on `analytics_536663906.events_*`.

Next 6h plan: either implement Q6.6b min-touch fixes, implement Q6.5b contrast token fixes, continue D6.Q6.7 dark-mode parity, or switch to D7 Lighthouse baseline while waiting for GA4 export tables.

EXPERIMENT E6.6 - Touch target audit

H tested: H6.6.1-H6.6.5

Method: grepped Dart UI code for explicit compact constraints, `shrinkWrap`, zero/minimum-size overrides, custom `GestureDetector`/`InkWell` controls, grid delegates, and sampled primary nav/quiz/kanji/lesson/library/practice surfaces.

Result: mobile nav height is `82`, Kanji quiz ratings are `84x44`, quiz next is `160x48`, Kanji grid cells use `70`, and radical grid cells use `88`. Clear risks remain: discover reorder `28/34` with `shrinkWrap`, discover focus chip small custom padding, top-bar notification `36/40`, mistake delete `36`, library/practice action CTAs with `minimumSize: Size(0, 0)`, home handwriting free-practice shrinkWrap, lesson inline icons with zero constraints, and interactive `StarRating`.

Surprise: moderate. The issue is broader than Discover Practice, but still concentrated in explicit compact patterns.

Update: define a `44x44` hitbox rule and fix compact controls by expanding invisible hit area, not visual chrome.

Action: wrote Q6.6 docs; no UI refactor yet.

Time: 0.4h

## 2026-05-14T15:58:00+07:00

Active dimension: D6

Active question: Q6.7

Hypothesis being tested: H6.7.1-H6.7.5

Experiment status: done

Commit hash: `e468d6c7`

Surprise log: Mild. Dark mode wiring and token tests are stronger than a pure feature-flag assumption, but route-level parity is still narrow.

Belief update: dark mode is functional, not polished. Fix parity through component-theme mirroring and route probes, not by removing the setting.

NS current value: real-user NS still unavailable; BigQuery auth is ready, and GA4 export is still waiting on `analytics_536663906.events_*`.

Next 6h plan: switch to D7 Lighthouse/performance baseline or implement the highest-risk D6b fixes if accessibility hardening takes priority.

EXPERIMENT E6.7 - Dark mode parity

H tested: H6.7.1-H6.7.5

Method: read shell/theme/provider/settings code, grepped hardcoded colors and brightness-aware branches, inspected hardcoded light-surface examples, then ran focused dark/theme tests.

Result: `MaterialApp.router` wires `darkTheme` and `themeMode`; Me settings persists dark/light; `AppTheme.dark` exposes `AppThemePalette.dark`; dark visual probe passed `1/1`; theme language tests passed `17/17`. Gaps remain: dark `ThemeData` is thinner than light, default mode is light-only not system, hardcoded light surfaces exist in Grammar repair prompts and Design Lab, broad color grep found `413` matches in `43` files, and only one route-level dark visual probe was found.

Surprise: mild. The foundation is usable, but not enough for a parity claim.

Update: keep dark mode as available; require route-level tests before marketing it as polished.

Action: wrote Q6.7 docs; no UI refactor yet.

Time: 0.4h

## 2026-05-14T16:12:00+07:00

Active dimension: D7

Active question: Q7.1

Hypothesis being tested: H7.1.1-H7.1.5

Experiment status: done

Commit hash: `e468d6c7`

Surprise log: Strong. The release build passes and app-shell gzip is manageable, but the first local route observed `250` resources and many grammar JSON fetches.

Belief update: D7 risk is less "Flutter web cannot build" and more "startup content loading needs a budget and lazy-loading policy."

NS current value: real-user NS still unavailable; BigQuery auth is ready, and GA4 export is still waiting on `analytics_536663906.events_*`.

Next 6h plan: either add Q7.1b perf tooling/budget or continue D8 release-risk audit while waiting for GA4 export.

EXPERIMENT E7.1 - Web performance baseline

H tested: H7.1.1-H7.1.5

Method: ran release web build, measured artifact sizes and gzip sizes, checked Lighthouse/browser availability, served `build/web` locally, then used Playwright performance/resource/console APIs as fallback.

Result: `flutter build web --release --base-href=/` passed in `51.9s`. `main.dart.js` is `6.25 MB` raw / `1.77 MB` gzip; `canvaskit.wasm` is `7.16 MB` raw / `2.92 MB` gzip; total `build/web` is `62.46 MB`; JSON assets total `19.35 MB`. Lighthouse CLI was not installed and Chrome/Edge were not on PATH. Playwright local release smoke loaded `JpStudy`, FCP was `248 ms`, resource count was `250`, and console had Firebase localhost referer `403` errors.

Surprise: strong. The structural performance blocker is broad initial content/resource loading, not the release build itself.

Update: define a budget around first-route resource count and route-level JSON bytes before optimizing visuals.

Action: wrote Q7.1 docs; no app code refactor yet.

Time: 0.6h

## 2026-05-14T16:45:00+07:00

Active dimension: D8

Active question: Q8.1

Hypothesis being tested: H8.1.1-H8.1.5

Experiment status: done

Commit hash: `aa5a2c441301bc43f7770325b41f0f4a7dd02bd7`

Surprise log: Moderate. Release web build is not the active blocker; release risk is stale live channels plus docs/config drift around App Check and CSP.

Belief update: before more live UAT, the release path needs a hygiene gate: exact deploy target, exact build flags, full local release gate, fresh deploy, then D4/D7 live probes.

NS current value: real-user NS still unavailable; BigQuery identity/job path works, but `analytics_536663906` is absent and Firebase datasets currently expose zero tables.

Next 6h plan: wait for GA4 export table; if still blocked, update shipping/security docs or continue D7.1b perf tooling.

EXPERIMENT E8.1 - Release risk inventory

H tested: H8.1.1-H8.1.5

Method: inspected Firebase Hosting config, `.firebaserc`, shipping/security docs, web shell, Firebase App Check activation, and live channel metadata; ran focused route/layout smoke tests; used read-only Firebase CLI commands only.

Result: `jpstudy-v2` live released `2026-05-13 01:32:40 +07`; `jpstudy` live released `2026-05-13 21:34:45 +07`; current `HEAD` is `2026-05-14T14:03:01+07:00`. Focused smoke tests pass after updating stale `/exam-center` expectation to `ExamCenterHubScreen`: route smoke `1/1`, layout smoke `1/1`, exam/coach smoke `4/4`. `SHIPPING.md` omits `JPSTUDY_RECAPTCHA_SITE_KEY`, while `lib/main.dart` activates web App Check only when that dart-define is present. Security checklist says strict CSP is not enabled, but `firebase.json` and live channels configure CSP.

Surprise: moderate. CI/local smoke risk was partly test drift, but deploy readiness is mostly documentation/channel drift.

Update: do not treat either live URL as current-source evidence until a fresh explicit deploy.

Action: wrote Q8.1 docs and patched the stale smoke assertion; no deploy attempted.

Time: 0.5h

## 2026-05-14T17:05:00+07:00

Active dimension: D8

Active question: Q8.1

Hypothesis being tested: H8.1.1-H8.1.5

Experiment status: done

Commit hash: `aa5a2c441301bc43f7770325b41f0f4a7dd02bd7`

Surprise log: Strong. Privacy/Terms are not hidden in settings or docs; the required in-app consent surface appears absent.

Belief update: D8.Q8.1 is not a legal-copy polish task yet. It first needs route constants, route screens, visible links, and smoke/widget tests.

NS current value: real-user NS still unavailable; BigQuery identity/job path works, but event tables remain absent.

Next 6h plan: implement minimal legal route/link slice or continue D8.Q8.2 API-key restriction audit while waiting for GA4 export.

EXPERIMENT E8.2 - Privacy and Terms coverage audit

H tested: H8.1.1-H8.1.5

Method: grepped source/docs/tests for privacy/terms/legal strings, then read route constants, meta routes, login dialog, onboarding screen, Me settings, and data controls.

Result: no `/privacy` or `/terms` route constants/routes found; no VI+EN legal copy found; `terms` hits are vocabulary count labels; no Settings/Data, Onboarding, or Login policy links found.

Surprise: strong. The compliance surface is absent, not merely stale.

Update: require route/link/test scaffolding before evaluating legal wording.

Action: wrote D8-compliance Q8.1 docs; no legal text drafted.

Time: 0.4h

## 2026-05-14T17:10:00+07:00

Active dimension: D8

Active question: Q8.1

Hypothesis being tested: H8.1.1-H8.1.5

Experiment status: implemented minimal slice

Commit hash: `aa5a2c441301bc43f7770325b41f0f4a7dd02bd7`

Surprise log: none new. The implementation matched E8.2: missing routes/links were the blocker.

Belief update: D8.Q8.1 moved from product-surface absent to legal-review blocked. The beta draft can be navigated and tested, but public launch still needs final copy/support/deletion approval.

NS current value: real-user NS still unavailable; BigQuery identity/job path works, but event tables remain absent.

Next 6h plan: continue D8 hardening or D7 perf tooling while waiting for GA4 export; after `analytics_536663906` appears, run the first real NS report.

EXPERIMENT E8.3 - Privacy and Terms minimal route/link slice

H tested: H8.1.1-H8.1.5

Method: added `/privacy` and `/terms` constants/routes, shared `LegalDocumentScreen` + `LegalDocumentLinks`, VI+EN `review-needed draft` legal copy, and links in Login, Onboarding, Me settings, and Data controls. Added route/widget tests for these surfaces.

Result: route smoke `1/1`, login dialog `14/14`, onboarding `3/3`, data settings `13/13` passed. Login dialog was made scrollable to prevent small-viewport overflow after adding legal links.

Update: legal route/link absence is cleared; legal sufficiency remains unapproved.

Action: implemented legal surface slice and updated D8-compliance docs.

Time: 0.5h

## 2026-05-14T17:14:00+07:00

Active dimension: D8

Active question: Q8.2

Hypothesis being tested: H8.2.1-H8.2.4

Experiment status: done

Commit hash: `aa5a2c441301bc43f7770325b41f0f4a7dd02bd7`

Surprise log: Moderate positive. The web Firebase API key is already referrer-restricted for Identity Toolkit, even though the security checklist did not explicitly record this gate.

Belief update: API-key restriction is not an active web Auth blocker. The remaining work is documentation and manual console verification before launch, plus separate App Check/Auth-domain/Storage controls.

NS current value: real-user NS still unavailable; BigQuery identity/job path works, but event tables remain absent.

Next 6h plan: continue D8 launch compliance with Auth authorized domains or release-doc/App Check parity while waiting for GA4 export.

EXPERIMENT E8.4 - Firebase web API-key fake-referrer probe

H tested: H8.2.1-H8.2.4

Method: read `lib/firebase_options.dart`, `firebase.json`, `.firebaserc`, and `docs/FIREBASE_SECURITY_CHECKLIST.md`; then sent Identity Toolkit probes with fake `Origin`/`Referer` and non-mutating hosting-referrer probes.

Result: fake `https://evil.example/probe` referrer returned `403 API_KEY_HTTP_REFERRER_BLOCKED` for `accounts:signUp` and `accounts:lookup`. Hosting referrers `https://jpstudy-v2.web.app/` and `https://jpstudy.web.app/` reached endpoint validation and returned `400 MISSING_ID_TOKEN`.

Surprise: moderate. The manual restriction appears active already; the repository lacked a repeatable verification gate.

Update: D8.Q8.2 measured as currently passing for web Identity Toolkit; still manually inspect GCP Console allowed-referrer list before launch.

Action: wrote Q8.2 docs and added an API-key restriction probe to `docs/FIREBASE_SECURITY_CHECKLIST.md`.

Time: 0.3h

## 2026-05-14T17:20:00+07:00

Active dimension: D8

Active question: Q8.3

Hypothesis being tested: H8.3.1-H8.3.4

Experiment status: done

Commit hash: `aa5a2c441301bc43f7770325b41f0f4a7dd02bd7`

Surprise log: none. Firebase Auth authorized domains are console-side; source can only verify intended hosts and docs.

Belief update: Q8.3 is a manual launch gate. Repository docs need to make the Console step explicit so `localhost` does not silently remain authorized in production.

NS current value: real-user NS still unavailable; BigQuery identity/job path works, but event tables remain absent.

Next 6h plan: continue D8 release-doc/App Check parity or D7 performance tooling while waiting for GA4 export.

EXPERIMENT E8.5 - Firebase Auth authorized domains source audit

H tested: H8.3.1-H8.3.4

Method: read `lib/firebase_options.dart`, `.firebaserc`, `firebase.json`, and security docs; ran `npx firebase hosting:sites:list --project jpstudy-v2 --json`; grepped source/docs for authorized-domain, OAuth, localhost, and Auth domain mentions.

Result: app source uses `authDomain: jpstudy-v2.firebaseapp.com`; Hosting CLI confirms `https://jpstudy.web.app` and `https://jpstudy-v2.web.app`. Firebase Auth authorized-domain allowlist and `localhost` removal are not exposed by source/hosting CLI. Security checklist only had a generic review line before E8.5.

Surprise: none.

Update: final Q8.3 proof requires Firebase Console inspection. Docs now carry the production-domain and `localhost` removal gate.

Action: wrote Q8.3 docs and expanded `docs/FIREBASE_SECURITY_CHECKLIST.md`.

Time: 0.2h

## 2026-05-14T17:25:00+07:00

Active dimension: D8

Active question: Q8.1 release-risk remediation

Hypothesis being tested: H8.1.4 follow-up

Experiment status: remediated docs

Commit hash: `aa5a2c441301bc43f7770325b41f0f4a7dd02bd7`

Surprise log: none. This was a known E8.1 doc/config drift.

Belief update: release-doc drift is reduced: the web build path now includes App Check, and the security checklist now matches CSP in `firebase.json`. Live channel freshness and post-deploy probes remain unproven.

NS current value: real-user NS still unavailable; BigQuery identity/job path works, but event tables remain absent.

Next 6h plan: continue D7 perf tooling or D8 monitoring/CI gates while waiting for GA4 export.

EXPERIMENT E8.6 - Release docs App Check/CSP remediation

H tested: H8.1.4 follow-up

Method: re-read `SHIPPING.md`, `docs/FIREBASE_SECURITY_CHECKLIST.md`, `firebase.json`, and `lib/main.dart`; patched docs to match actual App Check and CSP behavior.

Result: `SHIPPING.md` now requires `JPSTUDY_RECAPTCHA_SITE_KEY` for web release builds and explicit Hosting target selection. Security checklist now records CSP as enabled in `firebase.json` for both targets and requires header checks for both live URLs.

Surprise: none.

Update: release docs are less misleading, but no deploy was attempted and no live route/perf re-probe was run.

Action: patched `SHIPPING.md`, `docs/FIREBASE_SECURITY_CHECKLIST.md`, and D8 release-risk docs.

Time: 0.2h

## 2026-05-14T17:33:00+07:00

Active dimension: D8

Active question: Q8.5

Hypothesis being tested: H8.5.1-H8.5.5

Experiment status: measured and partially remediated

Commit hash: `aa5a2c441301bc43f7770325b41f0f4a7dd02bd7`

Surprise log: Moderate positive. CI is broader than the prompt implied: it already runs UI string guard, analyze, tests, web build, and Storage rules tests.

Belief update: CI is not the top release blocker for local source quality. The gap is deploy/liveness/performance automation: no live route smoke, no Lighthouse budget, no deploy workflow, no explicit notification.

NS current value: real-user NS still unavailable; BigQuery identity/job path works, but event tables remain absent.

Next 6h plan: continue D7 Lighthouse/perf budget tooling or D8 monitoring gate while waiting for GA4 export.

EXPERIMENT E8.7 - CI/CD gate audit

H tested: H8.5.1-H8.5.5

Method: listed `.github/workflows`, read `ui-string-guard.yml`, read `package.json`, and compared workflow gates to D8.Q8.5 requirements.

Result: GitHub Actions has a workflow on push/main/master and PR. It runs string guard, `flutter analyze`, `flutter test`, web build, `npm ci`, and `npm run test:storage-rules`. It lacked release-like web build flags, live smoke, Lighthouse, deploy automation, and explicit notification.

Surprise: moderate. CI was not absent; it was mislabeled and incomplete for deployment.

Update: renamed workflow to `CI`, changed web build to `flutter build web --release --base-href=/ --dart-define=JPSTUDY_RECAPTCHA_SITE_KEY=ci-placeholder`, and added the D7 web performance budget report after build. The same build command passed locally in `54.2s`.

Action: wrote Q8.5 docs and patched `.github/workflows/ui-string-guard.yml`.

Time: 0.3h

## 2026-05-14T17:45:00+07:00

Active dimension: D7

Active question: Q7.1b

Hypothesis being tested: H7.1.2-H7.1.4 follow-up

Experiment status: implemented budget gate

Commit hash: `aa5a2c441301bc43f7770325b41f0f4a7dd02bd7`

Surprise log: none. This was planned fallback tooling after Lighthouse was unavailable.

Belief update: D7 has a reproducible artifact-size gate now, but still lacks browser resource-count and Lighthouse/live timing gates.

NS current value: real-user NS still unavailable; BigQuery identity/job path works, but event tables remain absent.

Next 6h plan: extend D7 tooling to browser resource-count capture, or continue D8 monitoring while waiting for GA4 export.

EXPERIMENT E7.2 - Web build performance budget gate

H tested: H7.1.2-H7.1.4 follow-up

Method: used TDD to add `tool/research/web_perf_budget_report.dart`, `lib/core/research/web_perf_budget.dart`, and `docs/research/D7-performance/web_perf_budget.json`; then ran the CLI against current `build/web`.

Result: regression test passed `1/1`. Current build passed all budgets: `main.dart.js` `6,267,965` raw / `1,758,470` gzip; CanvasKit `7,155,780` raw / `2,899,774` gzip; total build `62,477,619`; total JSON `19,347,955`.

Surprise: none.

Update: build-size budget regression is now measurable; route-level resource count remains the next D7 blocker.

Action: added budget JSON, CLI, library, test, and D7 docs.

Time: 0.5h

## 2026-05-14T18:16:00+07:00

Active dimension: D7

Active question: Q7.1b

Hypothesis being tested: H7.1.3 follow-up

Experiment status: implemented lazy grammar seed remediation

Commit hash: `aa5a2c441301bc43f7770325b41f0f4a7dd02bd7`

Surprise log: Moderate. The broad grammar fetch was not only `ContentDatabase`; app startup also fired `GrammarSeeder.seedGrammarData()` and loaded every grammar level into the app DB.

Belief update: first-load perf risk is partly architectural: startup seeders must be level-scoped, and every non-active route that directly reads content must explicitly request its level.

NS current value: real-user NS still unavailable; BigQuery identity/job path works, but GA4 export tables remain absent.

Next 6h plan: rebuild web and add/run a browser resource-count smoke to compare against the `250` baseline; continue D8 live/release gates while waiting for `analytics_536663906`.

EXPERIMENT E7.3 - Lazy grammar seed remediation

H tested: H7.1.3 follow-up

Method: added RED tests for app grammar startup seeding and non-active lesson/JLPT grammar paths, then scoped `GrammarSeeder` and `ContentDatabase` grammar seeding by requested level.

Result: startup app grammar seed now reads `onboarding.level`; `GrammarRepository.fetchPointsByLevel`, `LessonRepository.seedGrammarIfEmpty`, and JLPT mock grammar sections seed requested levels on demand. Focused lazy-seed tests passed `12/12`; mock/coach route tests passed `33/33`; analyzer passed; D7 budget report passed.

Surprise: moderate. Fixing only content DB would have left the app DB startup seeder pulling all grammar JSON.

Update: all-level grammar startup seeding is fixed locally; route-level resource-count improvement still needs a fresh browser measurement.

Action: updated D7 docs and research index; no deploy attempted.

Time: 0.7h

## 2026-05-14T19:16:34+07:00

Active dimension: D7

Active question: Q7.2

Hypothesis being tested: H7.2.1-H7.2.4

Experiment status: measured

Commit hash: `239d42cc`

Surprise log: Moderate. Raw build size is dominated by CanvasKit/Skwasm renderer payloads (`52.6%`) and content/support JSON (`30.8%`), while top-level app JS is only `10.6%`. There are no deferred route chunks.

Belief update: route startup risk is now two-track: keep content lazy-loading for request count, but evaluate renderer mode/CDN strategy before chasing small Dart bundle reductions.

NS current value: real-user NS still unavailable; BigQuery identity/job path works, but GA4 export tables remain absent.

Next 6h plan: continue onboarding gate Phase 2, then D7.Q7.3 renderer choice. Live deploy remains blocked locally until `JPSTUDY_RECAPTCHA_SITE_KEY` is available.

EXPERIMENT E7.4 - Web bundle composition

H tested: H7.2.1-H7.2.4

Method: grouped `build/web` by extension, top asset path, largest files, deferred chunk names, and a separate `--source-maps` build in `build/web_q72_sourcemap`.

Result: total `build/web` is `62.48 MB` raw. CanvasKit/Skwasm files are `32.88 MB` (`52.6%`), content/support JSON is `19.24 MB` (`30.8%`), top-level app JS is `6.65 MB` (`10.6%`), and SQLite wasm/worker is `1.10 MB` (`1.8%`). No `part`, `deferred`, or `main.dart.js_` chunks were found. Source map has `1,464` sources, led by Flutter/framework, app features, Flutter engine, Dart SDK, Drift, SQLite/Riverpod/Firebase families.

Surprise: moderate. The raw build is more renderer/runtime-heavy than a "main.dart.js too large" model suggests.

Update: Q7.3 should test renderer choice; Q7.4 should test route deferral feasibility.

Action: wrote Q7.2 docs and updated the research index.

Time: 0.4h

## 2026-05-14T19:31:00+07:00

Active dimension: D7

Active question: Q7.3

Hypothesis being tested: H7.3.1-H7.3.4

Experiment status: measured

Commit hash: `60fbdfc9`

Surprise log: Moderate. `--wasm` builds successfully, but it emits Skwasm primary plus CanvasKit JS fallback and increases raw output by `9.4%`.

Belief update: do not switch renderer for beta based on local smoke. Keep default CanvasKit until live Hosting, browser matrix, and Lighthouse/trace data prove a better path.

NS current value: real-user NS still unavailable; BigQuery identity/job path works, but GA4 export tables remain absent.

Next 6h plan: Phase 3 mandatory boot gate, then D8.Q8.4 error monitoring design. Live deploy remains blocked locally until `JPSTUDY_RECAPTCHA_SITE_KEY` is available.

EXPERIMENT E7.5 - Renderer choice

H tested: H7.3.1-H7.3.4

Method: read official Flutter web renderer/Wasm docs, parsed default build config, built `--wasm` to `build/web_q73_wasm`, compared total bytes, top files, gzip sizes, local resource smokes, and current Firebase Hosting headers.

Result: default build config is `dart2js` + `canvaskit`. Wasm build config is primary `dart2wasm` + `skwasm` with fallback `dart2js` + `canvaskit`. Default build is `62.48 MB`; Wasm build is `68.34 MB` (`+9.4%`). Local smoke showed Wasm `21` resources/FCP `468ms` versus default `39`/`540ms`, but both included Kaspersky-injected zero-byte entries. Current Hosting CSP includes `'wasm-unsafe-eval'`; COOP/COEP headers are not present.

Surprise: moderate. Wasm is technically viable locally but not a clear beta win because fallback artifacts raise deploy size and live compatibility is still unproven.

Update: keep default renderer for beta; use Wasm only in a future preview-channel experiment.

Action: wrote Q7.3 docs and updated the research index.

Time: 0.5h
