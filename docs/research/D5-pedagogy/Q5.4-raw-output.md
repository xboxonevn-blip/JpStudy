# D5.Q5.4 Raw Output - Onboarding Fork Logic

## Focused Test Output

Command:

```text
flutter test --concurrency=1 test\features\onboarding\onboarding_screen_test.dart test\app\app_init_deep_link_test.dart test\core\study_goal_test.dart test\core\study_level_test.dart test\features\home\daily_plan_model_test.dart test\features\home\home_screen_test.dart
```

Output:

```text
All tests passed: 43
```

## Onboarding / Fork Code Search

Command:

```text
rg -n "enum StudyGoal|StudyGoal\.values|if \(goal == StudyGoal\.writing\)|getVocabByLevel\(|fallbackItems|items == null \|\| items.length < 3|ref\.read\(studyGoalProvider|prefOnboardingGoal|todayXp / 30|totalDue < 40|count: 5|count: 3" lib\core lib\features test\features test\core -g "*.dart"
```

Output excerpt:

```text
lib\core\study_goal.dart
4: enum StudyGoal { jlpt, reading, writing }

lib\core\onboarding_provider.dart
11: const prefOnboardingGoal = 'onboarding.goal';
33: final goalName = prefs.getString(prefOnboardingGoal);
35: final goal = StudyGoal.values.firstWhere(
39: ref.read(studyGoalProvider.notifier).state = goal;

lib\features\onboarding\onboarding_screen.dart
271: ...StudyGoal.values.map(
421: return ref.read(lessonRepositoryProvider).getVocabByLevel(level.shortLabel);
556: const fallbackItems = <VocabItem>[
583: final source = items == null || items.length < 3
584: ? fallbackItems

lib\features\home\home_screen.dart
236: await prefs.setString(prefOnboardingGoal, goal.name);
245: ref.read(studyGoalProvider.notifier).state = goal;
248: if (goal == StudyGoal.writing) {

lib\features\home\providers\daily_plan_provider.dart
264: if (totalDue < 40) {
268: count: 5,
280: count: 3,
293: count: 3,

lib\features\home\screens\learning_path_screen.dart
75: value: hasStartedToday ? (todayXp / 30).clamp(0.18, 1.0) : 0.08,
```

## Missing Time/Deadline Search

Command:

```text
rg -n -i "exam date|test date|deadline|cram|cramming|minutes per day|daily minutes|session length|daily target|time budget|study time|target date" lib\features\onboarding lib\features\home lib\core test\features\onboarding test\features\home test\core -g "*.dart"
```

Output:

```text
No matches. Exit code 1.
```

## Persona Intent / Accessibility Search

Command:

```text
rg -n -i "travel|fun|news|group study|study group|font size|text size|accessibility setting|reduce motion|disableAnimations" lib\features\onboarding lib\features\home lib\core test\features\onboarding test\features\home test\core -g "*.dart"
```

Output excerpt:

```text
lib\core\study_goal.dart
78: return 'Manga, news, books';

lib\core\accessibility\reduced_motion.dart
4: /// (OS-level accessibility setting, exposed via
25: MediaQuery.of(context).disableAnimations ? Duration.zero : normal;

test\features\onboarding\onboarding_screen_test.dart
185: 'disableAnimations=true -> all AnimatedContainer durations collapse to zero',
212: 'disableAnimations=false -> AnimatedContainer durations remain non-zero',
```

## Telemetry Search

Command:

```text
rg -n "onboarding_completed|logOnboardingCompleted|parameters.*goal|NorthStarFunnelEvaluator|onboarding_completed" lib\core test\core test\tool tool -g "*.dart"
```

Output excerpt:

```text
lib\core\analytics\analytics_service.dart
33: Future<void> logOnboardingCompleted({
37: return _logEvent('onboarding_completed', {'level': level, 'goal': goal});

lib\core\research\north_star_eval.dart
399: if (event.name == 'onboarding_completed') {

test\core\analytics\analytics_service_test.dart
36: await service.logOnboardingCompleted(level: 'N5', goal: 'jlpt');
47: final onboardingIndex = fake.events.indexOf('onboarding_completed');

test\core\research\north_star_eval_test.dart
304: parameters: const {'level': 'N5', 'goal': 'jlpt'},
322: parameters: const {'level': 'N4', 'goal': 'reading'},
```
