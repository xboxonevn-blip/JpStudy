# D5.Q5.5 Raw Output - Mistake Review and Ghost Algorithm

## Focused Test Output

Command:

```text
flutter test --concurrency=1 test\data\daos\mistake_dao_test.dart test\data\repositories\grammar_repository_test.dart test\features\grammar\ghost_practice_screen_test.dart test\features\grammar\ghost_review_screen_test.dart test\features\vocab\vocab_ghost_review_screen_test.dart test\features\mistakes\mistake_screen_test.dart test\features\home\weakness_radar_priority_test.dart
```

Output:

```text
All tests passed: 72
```

## Code Search Excerpts

Command:

```text
rg -n "ghostReviewsDue|markGhost|recordReview|addMistake|markCorrect|removeMistake|wrongCount|requiredCorrectStreak" lib\data lib\features test -g "*.dart"
```

Output excerpt:

```text
lib\data\daos\mistake_dao.dart
10: static const int requiredCorrectStreak = 2;
13: /// If it already exists, increments the wrongCount and updates the timestamp.
39: requiredCorrectStreak,
59: Future<void> markCorrect(String type, int itemId) async {
69: if (existing.wrongCount <= 1) {
70: await removeMistake(type, itemId);
78: wrongCount: Value(existing.wrongCount - 1),

lib\data\repositories\grammar_repository.dart
67: Future<void> recordReview({
82: ghostReviewsDue = 1;
85: if (ghostReviewsDue > 0) {
86: ghostReviewsDue = 0;

lib\features\grammar\screens\ghost_practice_screen.dart
189: Future<void> _markAsMastered(int grammarId) async {
193: .markGrammarAsMastered(grammarId);

lib\features\vocab\screens\vocab_ghost_review_screen.dart
47: await repo.markCorrect(type: 'vocab', itemId: _currentItem.id);
```

## 1-3-7 Due Logic

Source:

```text
lib\features\jlpt\models\jlpt_coach_models.dart
```

Observed behavior:

```text
age < 24h -> notDue
24h <= age < 72h -> due1d
72h <= age < 168h -> due3d
age >= 168h -> due7d
```

Weakness radar priority:

```text
score = wrongCount * 10
D1 bonus +50
D3 bonus +40
D7 bonus +30
fresh mistakes under 24h -> 0
```

## Grammar Ghost Split

Current provider path:

```text
grammarGhostsProvider -> GrammarRepository.fetchGhostPoints() -> grammar_srs_state.ghostReviewsDue > 0
```

Old/other path:

```text
LessonRepository.fetchGrammarGhosts() -> AttemptAnswer.isCorrect == false and Attempt.mode LIKE %grammar%
LessonRepository.markGrammarAsMastered() -> deletes incorrect AttemptAnswer rows
```

`GhostPracticeScreen._markAsMastered()` calls the old `LessonRepository.markGrammarAsMastered()` path, while the visible ghost list comes from `ghostReviewsDue`.
