# Research Journal - 2026-05-15

## 09:35 +07:00 - D8.Q8.7 Analytics Reset

- Active dimension: D8 compliance / telemetry retention.
- Action: added a Data controls action for user-triggered Analytics reset on the current device.
- Evidence: `AnalyticsService.resetAnalyticsData()` is covered by service tests, including the web-unsupported path; Data controls widget test verifies button, confirmation, service call, and success snackbar.
- Belief update: device-side reset is useful but not enough for web beta deletion compliance because Firebase Analytics web currently does not support `resetAnalyticsData()`.
- Next blocker: GA4 deletion runbook plus retention/BigQuery TTL proof.

## 2026-05-15T09:55:00+07:00

- Active dimension: D8 compliance / deletion operations.
- Action: added `docs/compliance/user-data-deletion-runbook.md` and linked it from D8.Q8.7 plus `SHIPPING.md`.
- Evidence: runbook is source-aligned to current Auth UID, Storage `users/{uid}` paths, GA4 property `536663906`, and BigQuery export dataset status.
- Belief update: deletion readiness is now an executable support workflow, but public launch still needs first-run proof plus GA4 retention and BigQuery TTL screenshots/metadata.

## 2026-05-15T10:18:00+07:00

- Active dimension: D8 compliance / deletion operations.
- Action: added a Data controls support ID action that copies the Firebase UID for signed-in users.
- Evidence: widget test covers rendering the Support ID row, copying `uid-1` to the clipboard, and showing the copied snackbar.
- Belief update: support deletion can now target UID-based Auth, Storage, GA4 `userId`, and BigQuery `user_id` records without asking learners to inspect browser storage.

## 2026-05-15T11:19:41+07:00

- Active dimension: D4 live UAT / D8 release channel parity.
- Action: manually deployed `47105e86` to `hosting:jpstudy`, then re-checked live upper-level vocab catalog availability.
- Evidence: primary `https://jpstudy.web.app/` returned `200`, legacy `https://jpstudy-v2.web.app/` returned `404`, live resource smoke returned `25` resources / `1` JSON / `0` grammar JSON, and screenshots show N3/N2/N1 Hajimete + Shin Kanzen tracks open.
- Belief update: the stale D4 upper-vocab blocker was deploy/channel parity plus catalog gating, not missing upper-level vocab data. Broad beta remains blocked by ops/legal/telemetry proofs.

## 2026-05-15T11:25:00+07:00

- Active dimension: D1 measurement / D8 telemetry retention.
- Action: re-ran BigQuery export checks with `tool/research/bigquery_rest_runner.js`.
- Evidence: `analytics_536663906` exists in `asia-southeast1` with `events_20260514`; 48h counts are `31` page views, `6` sessions, `1` onboarding completion; funnel is `4` opened / `1` onboarded / `0` first SRS; real NS is `0.00%`; dataset/table TTL is `60` days.
- Belief update: measurement is no longer blocked on export provisioning. It is blocked on collecting real post-onboarding learning events.
