# D7.Q7.7 Hypotheses - Kanji and Grammar Lazy-Load Verification

## H7.7.1 - Home stays clean

Prediction: an onboarded N4 user landing on `/` fetches no grammar, grammar-example, vocab, or kanji content JSON during the first 12 seconds.

Falsifier: any first-route study-content JSON appears on home.

## H7.7.2 - Grammar route is level-scoped

Prediction: direct `/grammar` for N4 fetches only N4 grammar JSON and N4 grammar example JSON.

Falsifier: it fetches N5/N3/N2/N1 grammar or unrelated vocab/kanji JSON.

## H7.7.3 - Kanji route is route-scoped

Prediction: direct `/kanji` for N4 fetches only Kanji route support/current-level kanji data.

Falsifier: it fetches grammar or vocab JSON on a Kanji-only route.

## H7.7.4 - Active-level seeding is better than all-level seeding

Prediction: if extra study-content JSON appears, it is scoped to the active level only.

Falsifier: any route pulls all JLPT levels.

## H7.7.5 - D7 still needs a live route budget

Prediction: route-specific resource counts differ enough that home-only resource smoke is insufficient.

Falsifier: home, grammar, and kanji routes have similar content-resource shape.
