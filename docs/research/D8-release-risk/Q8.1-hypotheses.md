# D8.Q8.1 Hypotheses - Release Risk Inventory

Question: Can the current web release path be trusted enough for beta sharing while GA4 export tables are still pending?

## H8.1.1 - Local release gates are executable

Prediction: the repo has a documented release gate, and focused route/layout smoke tests pass from the current workspace.

## H8.1.2 - Hosting targets are clear

Prediction: Firebase Hosting has two intended live sites, `jpstudy-v2` and `jpstudy`, and local `firebase.json` maps both.

## H8.1.3 - Live hosting is behind local source

Prediction: read-only channel metadata shows live releases older than current `HEAD`, explaining D4 live/local mismatches.

## H8.1.4 - Release security docs match config

Prediction: shipping/security docs, `firebase.json`, and live channel config agree on CSP/App Check expectations.

## H8.1.5 - Measurement remains release-blocked by data tables, not identity

Prediction: D1 BigQuery auth works, but GA4 `analytics_536663906.events_*` is still absent during the first export window.
