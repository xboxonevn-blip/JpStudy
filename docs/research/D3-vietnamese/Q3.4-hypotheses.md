# D3.Q3.4 Hypotheses - Vietnamese Typography Quality

Commit: `8387e444`

## Question

Do learner-facing Vietnamese strings follow Quốc Ngữ orthography and launch-acceptable typography?

## Hypotheses

H3.4.1: At least `85/100` fixed-seed sampled strings will score `5/5` under a conservative typography heuristic, because recent mojibake and glossary fixes removed the highest-risk defects.

H3.4.2: Raw English terms (`deck`, `quiz`, `review`, `lane`, `flow`, `block`, etc.) will appear in at least `10/100` sampled strings, because hardcoded feature copy still mixes English product jargon into Vietnamese UI.

H3.4.3: Tone-mark variant warnings (`thuỷ`, `huỷ`, `tuỳ`, `quí`, `qúy`) will appear in fewer than `3/100` sampled strings, because most UI copy uses common modern Vietnamese spellings.

H3.4.4: Punctuation/typography problems (`...`, ASCII dash, spaces before punctuation, question-mark mojibake) will appear in fewer than `5/100` sampled strings after the D3.Q3.3 encoding cleanup.

H3.4.5: Any detected issues will concentrate outside `app_language.dart`, because D3.Q3.2 showed the largest uncontrolled surface is hardcoded feature copy.
