# D3.Q3.1 Hypotheses - App Language Audit

## H3.1.1 - Key coverage is structurally balanced

Prediction: `lib/core/app_language.dart` has roughly equal `AppLanguage.en`, `AppLanguage.vi`, and `AppLanguage.ja` return counts because each getter/function uses switch cases for all three locales.

Falsifier: Vietnamese return count differs from English by more than 5%.

## H3.1.2 - Vietnamese terminology is inconsistent despite balanced keys

Prediction: even with balanced cases, the Vietnamese copy mixes "quiz", "thi thử", "ôn luyện", "review", "kanji", and "Hán Việt" inconsistently.

Falsifier: a scan finds no repeated mixed English/Vietnamese learning terms in Vietnamese strings.

## H3.1.3 - Empty/TODO Vietnamese app strings are rare

Prediction: missing Vietnamese in `app_language.dart` is more likely to be awkward/terminology debt than blank/TODO values.

Falsifier: more than 10 Vietnamese returns are empty or contain TODO/draft placeholders.
