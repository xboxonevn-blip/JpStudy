# D3.Q3.3 Hypotheses - Mojibake Scan

## H3.3.1 - Mojibake still exists in code comments/logs

Prediction: `lib/` still contains UTF-8 mojibake markers such as `â†`, `â‰`, `âœ`, or `â”`, mostly in comments/log strings rather than visible UI.

Falsifier: zero mojibake markers in Dart source.

## H3.3.2 - Content JSON mojibake is lower than prior audits suggest

Prediction: D2 cleanup removed many visible mojibake cases from content JSON, but not every marker family is gone.

Falsifier: more than 100 content JSON files contain mojibake markers.

## H3.3.3 - Docs contain historical mojibake/legacy examples

Prediction: docs include some mojibake or legacy encoding examples and should be classified separately from runtime source/content.

Falsifier: docs have zero mojibake markers.
