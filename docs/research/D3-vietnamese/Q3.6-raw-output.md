# D3.Q3.6 Raw Output - Plural and ICU Risk

## ICU Search

```text
rg -n "Intl\.plural|plural" lib -g "*.dart"

<no matches; exit 1>
```

## Manual Singular Guards

```text
rg -n '== 1 \? '''' : ''s''|== 1 \? "" : "s"|count == 1|activeCount == 1|completedCount == 1|length == 1' lib -g '*.dart'

9 matches in 7 files

lib/features/custom_decks/custom_decks_screen.dart:395:
'$activeCount active deck${activeCount == 1 ? '' : 's'} available right now.',

lib/features/custom_decks/custom_decks_screen.dart:415:
? 'Reuse patterns from $completedCount completed deck${completedCount == 1 ? '' : 's'} for a faster sprint.'

lib/features/home/widgets/next_step_suggestions.dart:267:
'${step.count} ${step.count == 1 ? 'item' : 'items'}',

lib/features/search/search_screen.dart:787:
return '$count result${count == 1 ? '' : 's'} for "$query"';

lib/features/search/search_screen.dart:831:
return '$count more result${count == 1 ? '' : 's'} ranked behind the top hit.';
```

Other matches were non-copy control-flow checks.

## Raw English Count Strings

```text
rg -n '\$count (items|terms|lessons|questions|strokes|due)|\$[a-zA-Z]+Count (items|terms|lessons|questions|decks)|\$\{[^}]+\} (items|terms|lessons|questions|decks|prompts)' lib -g '*.dart'

41 matches in 18 files
```

High-signal examples:

```text
lib/core/app_language.dart:281: return '$count terms';
lib/core/app_language.dart:1098: return '$count items';
lib/core/app_language.dart:1912: return '$count strokes';
lib/core/app_language.dart:2429: return 'Expected: $count strokes';
lib/core/app_language.dart:2770: return '$count items reviewed via SRS';
lib/core/app_language.dart:4013: return '$count lessons';
lib/core/app_language.dart:4759: return '$count terms available';
lib/core/app_language.dart:4770: return '$count questions available';
lib/core/app_language.dart:5364: return 'You have $count unanswered questions. Submit anyway?';
lib/core/app_language.dart:5529: return '$count terms need practice';
lib/core/app_language.dart:7033: return '$count terms due';
lib/features/custom_decks/custom_decks_screen.dart:388: '${continueCount == 0 ? dueTotal : continueCount} items need quick attention.',
lib/features/home/providers/coach_session_provider.dart:185: en: '${recoveryPack.itemCount} items to reinforce',
lib/features/immersion/immersion_home_screen.dart:306: 'Choose a level-based reading deck, tap words, and keep Japanese active. $readCount decks finished.',
lib/features/jlpt/screens/jlpt_mock_pro_screen.dart:368: '$questionCount questions • $totalMinutes minutes • $sectionCount sections',
lib/features/jlpt/screens/jlpt_reading_screen.dart:591: '${passage.level} • ${passage.questions.length} questions',
lib/features/library/library_screen.dart:1237: '${lesson.completedCount}/${lesson.termCount} terms are covered and ${lesson.dueCount} need review now.',
lib/features/test/screens/test_config_screen.dart:1116: '${examConfig.questionCount} questions',
lib/features/vocab/screens/hajimete_chapter_catalog_screen.dart:744: AppLanguage.en => '$count terms',
lib/features/vocab/screens/minna_lesson_catalog_screen.dart:776: AppLanguage.en => '$count lessons',
lib/features/vocab/vocab_copy.dart:291: AppLanguage.en => '$count terms',
```

## Interpolated String Surface

```text
(rg -n "\$\{[^}]+\}|\$[A-Za-z_][A-Za-z0-9_]*" lib -g "*.dart" | Measure-Object).Count

6942
```

## Post-Patch Rerun

After the central `AppLanguage` helper patch:

```text
rg -n '\$count (items|terms|lessons|questions|strokes|due)|\$[a-zA-Z]+Count (items|terms|lessons|questions|decks)|\$\{[^}]+\} (items|terms|lessons|questions|decks|prompts)' lib -g '*.dart'

31 matches in 18 files
```

After the 2026-05-15 `VocabCopy` follow-up:

```text
rg -n '\$count (items|terms|lessons|questions|strokes|due)|\$[a-zA-Z]+Count (items|terms|lessons|questions|decks)|\$\{[^}]+\} (items|terms|lessons|questions|decks|prompts)' lib -g '*.dart'

29 matches in 17 files
```
