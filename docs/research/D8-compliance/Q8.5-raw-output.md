# D8.Q8.5 Raw Output - CI/CD Gates

## Workflow Files

Command:

```text
Get-ChildItem .github\workflows -Force
```

Output:

```text
ui-string-guard.yml
```

## Existing Workflow

Source: `.github/workflows/ui-string-guard.yml`

```text
name before E8.7 -> UI String Guard
on -> push main/master, pull_request

job ui-string-guard:
- checkout
- subosito/flutter-action@v2, stable
- flutter pub get
- python tooling/audit_ui_string_literals.py --check --max-candidates 0
- flutter analyze
- flutter test
- flutter build web

job firebase-security-rules:
- checkout
- actions/setup-node@v4, node-version 24, npm cache
- npm ci
- npm run test:storage-rules
```

## Package Scripts

Source: `package.json`

```text
test:storage-rules -> firebase emulators:exec --only storage "jest tooling/firebase/storage.rules.test.js --runInBand"
test:web-resource-smoke -> node tool/research/web_resource_smoke.js --build-root build/web --max-resources 80 --max-json 5 --max-grammar 0
test:web-resource-smoke:unit -> node --test test/tool/research/web_resource_smoke_node_test.js
```

## Missing From Workflow Definition

```text
flutter build web --release --base-href=/ --dart-define=JPSTUDY_RECAPTCHA_SITE_KEY=... -> not found
Lighthouse -> not found
live URL smoke after deploy -> not found
firebase deploy workflow -> not found
Discord/Slack/email notification -> not found
branch protection settings -> not inspectable from repository files
```

## Current Interpretation Boundary

```text
Local PR quality gates -> present
Storage rules test gate -> present
Release-parity web build gate -> partial
Post-deploy live gate -> absent
Performance/Lighthouse gate -> absent
Notification gate -> unknown/absent in repo
```

## E8.7 CI Remediation

```text
workflow name -> CI
Build web command -> flutter build web --release --base-href=/ --dart-define=JPSTUDY_RECAPTCHA_SITE_KEY=ci-placeholder
Web perf budget command -> dart run tool/research/web_perf_budget_report.dart --build-root build/web --budget docs/research/D7-performance/web_perf_budget.json --fail-on-violation
Web resource smoke unit -> npm run test:web-resource-smoke:unit
Playwright browser install -> npx playwright install chromium
Web resource smoke command -> npm run test:web-resource-smoke
Deploy workflow -> still not added
Lighthouse workflow -> still not added
Live URL smoke -> still not added
```

Local verification:

```text
flutter build web --release --base-href=/ --dart-define=JPSTUDY_RECAPTCHA_SITE_KEY=ci-placeholder -> pass
compile time -> 54.2s
output -> build\web
npm run test:web-resource-smoke:unit -> pass 2/2
npm run test:web-resource-smoke -> pass, resourceCount 39, jsonCount 1, grammarResourceCount 0
```

## E8.12 Latest CI Run

```text
GitHub Actions run -> 25903240127
Commit -> 411db9ba0aa402b2da73defd1330ff8310adc3af
Result -> completed / success

firebase-security-rules -> completed / success
ui-string-guard -> completed / success
deploy-hosting -> completed / success wrapper

deploy-hosting skipped steps because deploy secrets are missing:
- Run actions/checkout@v4
- Run subosito/flutter-action@v2
- Run actions/setup-node@v4
- Install dependencies
- Install Node dependencies
- Build web for production
- Deploy primary Firebase Hosting target
- Smoke primary and legacy hosting
- Install Playwright Chromium
- Check live web resource smoke
- Lighthouse live gate

Workflow change in 411db9ba:
- Check deploy secrets now writes a GitHub Step Summary for both skipped and
  enabled deploy paths.
- The skipped summary links
  docs/compliance/beta-launch-proof-checklist-2026-05-15.md.
```

## Live Host Re-Check - 2026-05-15T14:02+07

```text
Primary URL -> https://jpstudy.web.app/?live-proof=<timestamp>
primary_status=200 length=6847

Legacy URL -> https://jpstudy-v2.web.app/?legacy-disabled-check=<timestamp>
legacy_status=404

node tool\research\web_resource_smoke.js --url https://jpstudy.web.app/ --max-resources 100 --max-json 8 --max-grammar 0 --json

title -> JpStudy
resourceCount -> 31
jsonCount -> 1
grammarResourceCount -> 0
violations -> []
largest app asset -> main.dart.js transferSize 1361146, decodedBodySize 6532748
```

Note: this is a live-host smoke proof, not a proof that the latest `main`
commit was deployed. The `deploy-hosting` GitHub Actions path still needs a
first secret-backed run.

## Firebase Token Generation Attempt - 2026-05-15

Command:

```powershell
firebase login:ci
```

Output:

```text
Error: Cannot run login:ci in non-interactive mode.
```

Interpretation: Codex cannot generate the CI token in this session. The project
owner must run `firebase login:ci` in an interactive terminal, then store the
result as GitHub repository secret `FIREBASE_TOKEN`.

## E8.13 CI Queue Remediation - 2026-05-15T15:32+07

```text
Commit -> af97a9c8 ci: allow manual main deploy verification
Change -> added workflow_dispatch so the owner can run CI manually on main
after setting deploy secrets.

Commit -> 0b44ff3e ci: bound workflow runtime on main
Change -> added workflow concurrency plus job timeouts.
Result -> GitHub Actions run 25907360025 completed failure because
ui-string-guard never received a runner; firebase-security-rules succeeded and
deploy-hosting skipped.

Commit -> b8d21ecd ci: serialize source checks on GitHub runners
Change -> firebase-security-rules now needs ui-string-guard, avoiding parallel
runner contention on the repository.

GitHub Actions run -> 25907947623
Commit -> b8d21ecdc733919ff695f1071377e101db743dfb
Result -> completed / success

ui-string-guard -> completed / success
firebase-security-rules -> completed / success
deploy-hosting -> completed / success wrapper

deploy-hosting skipped real deploy steps because required deploy secrets are
still missing:
- Build web for production
- Deploy primary Firebase Hosting target
- Smoke primary and legacy hosting
- Check live web resource smoke
- Lighthouse live gate
```

Interpretation: source CI is green again on `main` after serializing the
runner-heavy jobs. This still does not close the launch gate for automated
deploy because the first secret-backed deploy/live-smoke/Lighthouse path has
not run.

## GitHub Actions Secrets Metadata Check - 2026-05-15T15:48+07

Command shape:

```powershell
GET https://api.github.com/repos/xboxonevn-blip/JpStudy/actions/secrets
```

Output:

```json
{
  "total_count": 0,
  "names": null
}
```

Interpretation: this confirms the deploy skip cause directly. The repository
currently has no Actions secrets, so `FIREBASE_TOKEN`,
`JPSTUDY_RECAPTCHA_SITE_KEY`, and optional `JPSTUDY_SENTRY_DSN` still need to
be added by the owner before the real deploy job can run.

## GitHub Actions Secret Update - 2026-05-15T16:00+07

Using the local `JPSTUDY_RECAPTCHA_SITE_KEY` user env var and the repository
Actions public key, Codex set the `JPSTUDY_RECAPTCHA_SITE_KEY` repository
secret without printing the token or secret value.

Verification:

```json
{
  "total_count": 1,
  "names": "JPSTUDY_RECAPTCHA_SITE_KEY"
}
```

Interpretation: App Check site key is now available to GitHub Actions. The
real deploy path still cannot run because `FIREBASE_TOKEN` is missing. Optional
Sentry proof still needs `JPSTUDY_SENTRY_DSN`.

## GitHub Actions Secret Metadata Re-Check - 2026-05-15T16:52+07

Command shape:

```powershell
GET https://api.github.com/repos/xboxonevn-blip/JpStudy/actions/secrets
```

Output:

```json
{
  "total_count": 2,
  "names": "FIREBASE_TOKEN, JPSTUDY_RECAPTCHA_SITE_KEY"
}
```

Interpretation: required deploy secrets are now present. Optional
`JPSTUDY_SENTRY_DSN` is still not set/proven.

## Secret-Backed CI Deploy Proof - 2026-05-15T16:52+07

```text
GitHub Actions run -> 25911033019
URL -> https://github.com/xboxonevn-blip/JpStudy/actions/runs/25911033019
Commit -> 8eef59f7 test(tooling): stabilize content status CLI test cwd
Result -> completed / success

ui-string-guard -> completed / success
firebase-security-rules -> completed / success
deploy-hosting -> completed / success

deploy-hosting real deploy steps:
- Check deploy secrets -> success
- Build web for production -> success
- Deploy primary Firebase Hosting target -> success
- Smoke primary and legacy hosting -> success
- Check live web resource smoke -> success
- Lighthouse live gate -> success
```

Local live smoke after CI deploy:

```text
Primary URL -> https://jpstudy.web.app/
primary_status=200

Legacy URL -> https://jpstudy-v2.web.app/
legacy_status=404

node tool\research\web_resource_smoke.js --url https://jpstudy.web.app/ --max-resources 100 --max-json 8 --max-grammar 0 --json

title -> JpStudy — JLPT learning, FSRS spaced repetition, kanji practice
resourceCount -> 25
jsonCount -> 1
grammarResourceCount -> 0
violations -> []
largest app asset -> main.dart.js transferSize 1361723, decodedBodySize 6537860
```

Interpretation: first secret-backed automated deploy/live-smoke/Lighthouse gate
is now proven on `main`. This closes the previous CI/CD deploy blocker, but
does not close Sentry, Storage, legal, deletion, GA4 UI retention, or future
App Check enforcement proof gaps.

## Latest CI Recheck - 2026-05-15T18:31+07

```text
GitHub Actions run -> 25914760009
URL -> https://github.com/xboxonevn-blip/JpStudy/actions/runs/25914760009
Commit -> 5a19cd80 feat(observability): add sentry smoke event trigger
Result -> completed / success

ui-string-guard -> completed / success
firebase-security-rules -> completed / success
deploy-hosting -> completed / success
```

Interpretation: the latest `main` commit, including the disabled-by-default
Sentry smoke trigger, passed the same source gates and real
deploy/live-smoke/Lighthouse path.

## Latest CI Recheck - 2026-05-15T23:51+07

```text
GitHub Actions run -> 25929278168
URL -> https://github.com/xboxonevn-blip/JpStudy/actions/runs/25929278168
Commit -> 3cb00b1f i18n(ui): clear English plural-risk labels
Result -> completed / success

ui-string-guard -> completed / success
firebase-security-rules -> completed / success
deploy-hosting -> completed / success
```

Live host re-check after the latest successful run:

```text
Primary URL -> https://jpstudy.web.app/
primary_status=200

Legacy URL -> https://jpstudy-v2.web.app/
legacy_status=404

node tool\research\web_resource_smoke.js --url https://jpstudy.web.app/ --max-resources 100 --max-json 8 --max-grammar 0 --json

resourceCount -> 17
jsonCount -> 1
grammarResourceCount -> 0
violations -> []
largest app asset -> main.dart.js transferSize 1364030, decodedBodySize 6552224
```

Interpretation: the latest `main` source gate and real deploy-hosting path are
green. This still does not close Sentry, Storage, legal, deletion, GA4 UI
retention, real learning-event sample, or future App Check enforcement proof
gaps.

## Latest CI Recheck - 2026-05-16T00:28+07

```text
GitHub Actions run -> 25930999698
URL -> https://github.com/xboxonevn-blip/JpStudy/actions/runs/25930999698
Commit -> ebb425a9 test(tooling): harden dart CLI report lock
Result -> completed / success

ui-string-guard -> completed / success
firebase-security-rules -> completed / success
deploy-hosting -> completed / success
```

Live host re-check after the latest successful run:

```text
Primary URL -> https://jpstudy.web.app/
primary_status=200

Legacy URL -> https://jpstudy-v2.web.app/
legacy_status=404

node tool\research\web_resource_smoke.js --url https://jpstudy.web.app/ --max-resources 100 --max-json 8 --max-grammar 0 --json

resourceCount -> 27
jsonCount -> 1
grammarResourceCount -> 0
violations -> []
largest app asset -> main.dart.js transferSize 1364165, decodedBodySize 6552224
```

Interpretation: latest `main` CI/CD proof remains green after hardening the
Dart CLI report tests. Non-CI launch blockers remain unchanged.

## Latest CI Recheck - 2026-05-16T01:19+07

```text
GitHub Actions run -> 25933463058
URL -> https://github.com/xboxonevn-blip/JpStudy/actions/runs/25933463058
Commit -> 316f80c8 test(tooling): hold CLI lock until dart run exits
Result -> completed / success

ui-string-guard -> completed / success
firebase-security-rules -> completed / success
deploy-hosting -> completed / success
```

Local live host re-check after the latest successful run:

```text
Primary URL -> https://jpstudy.web.app/
primary_status=200

Legacy URL -> https://jpstudy-v2.web.app/
legacy_status=404
```

Interpretation: latest `main` CI/CD proof remains green after the Dart CLI lock
fix and docs proof refresh. Non-CI launch blockers remain unchanged.
## CI Recheck - 2026-05-16T08:12+07

Latest `main` proof:

```text
Commit -> 12a0b7ea
Run -> https://github.com/xboxonevn-blip/JpStudy/actions/runs/25948482462
ui-string-guard -> success
firebase-security-rules -> success
deploy-hosting -> success
```

Local/live follow-up in the same window:

```text
flutter build web --release --dart-define=JPSTUDY_RECAPTCHA_SITE_KEY=... -> success
firebase deploy --only hosting:jpstudy -> success
Live Playwright visual smoke -> Kanji radical headers, Han-Viet rules, Review Forecast labels
```

Interpretation: secret-backed CI deploy is still proven on current `main`.
Remaining D8 gates are external proof gates, not CI plumbing.
