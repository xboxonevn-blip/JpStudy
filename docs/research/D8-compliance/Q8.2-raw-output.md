# D8.Q8.2 Raw Output - Firebase API Key Restriction

## Source Facts

Source: `lib/firebase_options.dart`

```text
projectId -> jpstudy-v2
web appId -> 1:129949648924:web:a4dbfa27af569b478337d9
web authDomain -> jpstudy-v2.firebaseapp.com
web measurementId -> G-PKT7ELMCHR
web apiKey -> AIzaSyCgerXOA8C9qtjB1yP3oAbsvMAjpFHPSmc
windows apiKey -> same as web key
android apiKey -> separate Android key
iOS apiKey -> separate iOS key
```

Sources: `.firebaserc`, `firebase.json`

```text
Firebase project -> jpstudy-v2
Hosting targets -> jpstudy-v2, jpstudy
Expected live origins -> https://jpstudy-v2.web.app, https://jpstudy.web.app
```

Source: `docs/FIREBASE_SECURITY_CHECKLIST.md`

```text
App Check documented -> yes
Auth hardening documented -> yes
Explicit API-key HTTP referrer restriction gate -> not found before E8.4
```

## Fake Referrer Probe

Headers:

```text
Origin: https://evil.example
Referer: https://evil.example/probe
Content-Type: application/json
```

Probe: `POST https://identitytoolkit.googleapis.com/v1/accounts:signUp?key=<web-api-key>`

```text
status=403
message=Requests from referer https://evil.example/probe are blocked.
reason=API_KEY_HTTP_REFERRER_BLOCKED
service=identitytoolkit.googleapis.com
consumer=projects/129949648924
```

Probe: `POST https://identitytoolkit.googleapis.com/v1/accounts:lookup?key=<web-api-key>`

```text
status=403
message=Requests from referer https://evil.example/probe are blocked.
reason=API_KEY_HTTP_REFERRER_BLOCKED
service=identitytoolkit.googleapis.com
consumer=projects/129949648924
```

## Expected Hosting Referrer Probe

Non-mutating probe only: `POST accounts:lookup` with body `{}`.

Headers:

```text
Origin: https://jpstudy-v2.web.app
Referer: https://jpstudy-v2.web.app/
```

Result:

```text
status=400
message=MISSING_ID_TOKEN
```

Headers:

```text
Origin: https://jpstudy.web.app
Referer: https://jpstudy.web.app/
```

Result:

```text
status=400
message=MISSING_ID_TOKEN
```

Interpretation note for raw output: `400 MISSING_ID_TOKEN` is endpoint validation after the key/referrer layer. It is not a successful Auth action and did not include a user token.
