# D6.Q6.3 Experiment - Loading State Audit

## Experiment E6.3

Question: Are loading states clear, consistent, and proportional across major async surfaces?

Method:

- Search feature/core code for `CircularProgressIndicator`, `LinearProgressIndicator`, skeleton/shimmer patterns, and explicit loading copy.
- Inspect loading behavior for primary routes, optional home/me dashboard cards, progress panels, search, vocab, grammar, kanji, JLPT, and practice sessions.
- Compare blocking practice/session routes against optional dashboard panels.
- Reuse Q6.2 empty-state evidence for hidden loading branches that use `SizedBox.shrink()`.

Commands:

```text
rg -n "CircularProgressIndicator|LinearProgressIndicator|Skeleton|Shimmer|shimmer" lib\features lib\core lib\app -g "*.dart"
rg -n "CircularProgressIndicator\(" lib\features lib\core lib\app -g "*.dart"
rg -n "LinearProgressIndicator\(" lib\features lib\core lib\app -g "*.dart"
rg -n "Skeleton|Shimmer|shimmer" lib\features lib\core lib\app -g "*.dart"
rg -n "loading:\s*\(\)\s*=>\s*const SizedBox\.shrink\(\)|loading:\s*\(\)\s*=>\s*SizedBox\.shrink\(" lib\features lib\core lib\app -g "*.dart"
rg -n "Loading|loading|Đang tải|読み込み" lib\features lib\core lib\app -g "*.dart"
```

Fixed local commit baseline: `e468d6c7ff3af9ebbe31abc5889efb7dfddab51a`
