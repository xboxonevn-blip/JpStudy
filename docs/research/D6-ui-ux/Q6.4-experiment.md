# D6.Q6.4 Experiment - Error State Audit

## Experiment E6.4

Question: Are error states recoverable, localized, and appropriate to the surface severity?

Method:

- Search feature/core code for shared `ErrorStateWidget` usage.
- Search async `error:` branches for retry affordances, `loadErrorLabel`, raw exception rendering, and hidden errors.
- Inspect representative primary routes, optional dashboard widgets, feature-specific error cards, and action-level snackbar handling.
- Reuse Q6.2/Q6.3 evidence for collapsed optional panel states.

Commands:

```text
rg -n "ErrorStateWidget\(" lib\features lib\core lib\app test -g "*.dart"
rg -n "error:\s*\(|loadErrorLabel|Error loading|Failed to|failed to|Retry|retry|onRetry|invalidate\(" lib\features lib\core lib\app -g "*.dart"
rg -n "error:\s*\([^=]|catch \(|catch\(|SnackBar|showDialog<|showDialog\(" lib\features lib\core lib\app -g "*.dart"
rg -n "SizedBox\.shrink\(\)|return null;|debugPrint\(|print\(" lib\features lib\core lib\app -g "*.dart"
rg -n -C 4 "error: \(_, _\) => const SizedBox\.shrink\(\)|error: \(.*,.*\) => const SizedBox\.shrink\(\)" lib\features\home lib\features\me lib\features\progress lib\features\vocab -g "*.dart"
rg -n -C 3 "error: \(error, stack\) => Center\(child: Text\(error\.toString\(\)\)\)|error: \(e, st\) => Center\(child: Text\(e\.toString\(\)\)\)|Text\('\$\{language\.loadErrorLabel\}:" lib\features -g "*.dart"
```

Fixed local commit baseline: `e468d6c7ff3af9ebbe31abc5889efb7dfddab51a`
