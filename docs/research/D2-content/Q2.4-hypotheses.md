# D2.Q2.4 Hypotheses - Cross-Link Coverage Graph

## H2.4.1 - Grammar examples are mostly linked to grammar points

Prediction: grammar example groups match local grammar point titles for at least 95% of groups per level. Effect size: each level has >= 95% matched example groups.

Falsifier: any level has < 80% grammar example groups matching a grammar point.

## H2.4.2 - Vocab to kanji coverage is shallow

Prediction: many vocab entries contain kanji that are not represented in the local kanji lesson dataset, especially N1-N3. Effect size: fewer than 40% of kanji-bearing vocab entries are fully covered by same-level kanji entries.

Falsifier: most kanji-bearing vocab entries are fully covered by local kanji entries.

## H2.4.3 - Kanji example links exist but schema differs by level

Prediction: upper levels use example `word` links, while lower levels use `sourceVocabId`; at least one level has broken or namespace-mismatched refs. Effect size: link format differs by level and at least one level has < 95% refs found in vocab.

Falsifier: all levels use the same link schema and all refs resolve.
