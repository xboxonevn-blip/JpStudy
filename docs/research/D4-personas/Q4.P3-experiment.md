# D4.Q4.P3 Experiment — Mai N2

Commit: `8387e444`
Date: 2026-05-14
Live target: `https://jpstudy-v2.web.app`

## Setup

- Browser: Playwright Chromium.
- Desktop viewport: `1366x768`.
- Mobile spot-check viewport: `390x844` only if a desktop blocker is not already decisive.
- Pref seed:

```js
localStorage.clear();
localStorage.setItem('flutter.onboarding.completed', JSON.stringify(true));
localStorage.setItem('flutter.onboarding.level', JSON.stringify('n2'));
localStorage.setItem('flutter.onboarding.goal', JSON.stringify('jlpt'));
localStorage.setItem('flutter.app.locale', JSON.stringify('vi'));
localStorage.setItem('flutter.analytics.consent', JSON.stringify(false));
```

## Flow

1. A1 Root cold start: open `/#/`; record level, language, plan length, and obvious next action.
2. A2 Direct grammar: hard reload `/#/grammar`; record level and deck count.
3. B1 N2 grammar throughput: enter grammar practice or detail if reachable without mutation.
4. C1 N2 vocab/kanji confidence: open `/#/vocab` and `/#/kanji`; record content counts and empty-state issues.
5. D1 JLPT coach: open `/#/jlpt/coach`; record N2 readiness, mock length, reading count, and repair plan.
6. D2 Exam center: open `/#/exam-center`; record whether live route is rich hub or stale mock screen.
7. E1 Reading drill: open `/#/jlpt/reading`; record N2 sets, time target, CTA visibility.
8. F1 Group/social: open `/#/community` and `/#/leaderboard`; record invite/share/friend-study affordances.

## Evidence

- Save screenshots to `tests/uat-p3-2026-05-14/`.
- Raw output in `Q4.P3-raw-output.md`.
- Analysis in `Q4.P3-analysis.md`.
- Session note in `docs/notes/2026-05-14-uat-mai-n2-session.md`.

## Constraints

- No quiz submissions, no account mutations, no admin data export.
- Treat live deploy drift as production evidence; separate local-code fixes from live-channel issues.
