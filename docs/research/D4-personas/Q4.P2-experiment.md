# D4.Q4.P2 Experiment — Anh Tuấn N3 UAT

Commit: `8387e444`
Live target: `https://jpstudy-v2.web.app`
Date: 2026-05-14

## Setup

- Browser: Playwright MCP.
- Desktop viewport: `1366x900`.
- Mobile viewport: `390x844`.
- State seed: SharedPreferences web keys in `localStorage`.
- Correct web encoding: booleans as JSON booleans, strings as JSON strings.

```js
localStorage.setItem('flutter.onboarding.completed', JSON.stringify(true));
localStorage.setItem('flutter.onboarding.level', JSON.stringify('n3'));
localStorage.setItem('flutter.onboarding.goal', JSON.stringify('jlpt'));
localStorage.setItem('flutter.app.locale', JSON.stringify('vi'));
localStorage.setItem('flutter.analytics.consent', JSON.stringify(false));
```

## Method

1. Verify prefs encoding and cold-start behavior.
2. Run desktop route smoke: home, grammar, direct grammar deep link, exam center, JLPT coach, full mock, reading drill.
3. Run mobile route smoke: home, grammar, reading, kanji, vocab.
4. Avoid submitting quizzes/reviews or mutating production progress.
5. Save screenshots under `tests/uat-p2-2026-05-14/`.

## A-H Flow Mapping

A. Entry/onboarding state: seeded N3/JLPT/VI, root and direct route cold starts.
B. Home/roadmap: 15-minute plan and next action clarity.
C. Grammar N3: hub, counts, drill entry.
D. JLPT prep: coach, full mock, reading drill.
E. Vocab/Kanji: N3 lanes and soft-gate behavior.
F. Persistence/safety: reload/deep-link behavior, no deep progress mutation.
G. Mobile ergonomics: bottom nav, visible CTAs, modal interruptions.
H. Editorial/performance smoke: visible mojibake, language leakage, fatal console symptoms.

