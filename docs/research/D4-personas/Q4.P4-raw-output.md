# D4.Q4.P4 Raw Output - Bác Hùng N4 UAT

Commit: `e468d6c7`
Date: 2026-05-14
Live target: `https://jpstudy-v2.web.app`

## Storage Evidence

SharedPreferences-web JSON string storage:

```json
{
  "flutter.onboarding.completed": "true",
  "flutter.onboarding.level": "\"n4\"",
  "flutter.onboarding.goal": "\"reading\"",
  "flutter.app.locale": "\"vi\"",
  "flutter.analytics.consent": "false"
}
```

There is no `travel` study goal in `lib/core/study_goal.dart`; available values are `jlpt`, `reading`, and `writing`. P4 used `reading` as the closest low-pressure proxy.

## Screenshots

- `tests/uat-p4-2026-05-14/p4-root-tablet-125.png`
- `tests/uat-p4-2026-05-14/p4-root-after-slow-tap.png`
- `tests/uat-p4-2026-05-14/p4-kanji-tablet-125.png`
- `tests/uat-p4-2026-05-14/p4-vocab-tablet-125.png`
- `tests/uat-p4-2026-05-14/p4-grammar-tablet-125.png`
- `tests/uat-p4-2026-05-14/p4-reading-tablet-125.png`
- `tests/uat-p4-2026-05-14/p4-study-hub-tablet-125.png`
- `tests/uat-p4-2026-05-14/p4-community-tablet-125.png`

## Observations

| Flow | Raw observation |
| --- | --- |
| Root tablet at 125% | Root shows N4 chips and Vietnamese UI. It uses the compact/mobile shell at `834x1194`, with large stacked cards: `Nền tảng`, `Bảng kế hoạch hôm nay`, `Học từ vựng`, `Học ngữ pháp`, `Học Kanji`. No dense desktop plan is visible. |
| Slow tap on `Học ngay` | A slow single tap opens the `Học` surface. It keeps N4 context: `Đào sâu thêm vào N4`, next action `Bắt đầu Minna No Nihongo 26`, `Mở bài học`, `Ôn thi JLPT`, and a `~15p` plan step. |
| Root plan after tap | The `Học` surface includes `Mở immersion`, which is the clearest non-exam/reading-friendly path for P4. It also includes `Làm một block đề N4`, so exam prep remains prominent. |
| Direct kanji | Hard-loaded `/#/kanji` falls to N5. Copy says `Dùng N5 làm lane chính`; N5 tab selected; N4 tab visible but not selected. Foundations modal appears. |
| Direct vocab | Hard-loaded `/#/vocab` falls to N5. It shows `Lane hiện tại N5`, `Ready now`, `Hajimete N5 (0 mục từ)`, and foundations modal. |
| Direct grammar | Hard-loaded `/#/grammar` falls to `Lane N5`, `Xây nền ngữ pháp N5 thật vững`, `114` items, and foundations modal. |
| Direct reading | Hard-loaded `/#/jlpt/reading` falls to `Lộ trình N5`, `25 bộ đọc`, `3 câu mỗi bài`, `Mục tiêu 5 phút`; first cards are tagged N5. |
| Study hub | Hard-loaded `/#/study-hub` falls to N5 exam prep: `Ôn thi JLPT`, N5 chip, `Start prep`. Resource filters include `Đọc hiểu`, `Tự học`, and `Công cụ`, but first recommended cards are N5/JLPT. |
| Community/settings | `/#/community` is readable at 125% and exposes `Hồ sơ và lịch sử`, `Dữ liệu và reset`, and `Phòng thí nghiệm UI`. No font-size/accessibility setting is visible. |

## Code/Channel Check

The P4 direct-route N5 fallback matches P3 and the known P2 live-channel drift. Current local code already watches `appInitProvider` at the app shell and has a direct-grammar regression test.

The large-font preference has no app-level persisted setting. Code search found reduced-motion support, but no text-scale/font-size preference path.

## Code Action From UAT

No local code patch was made for P4. The evidence queues three decisions:

- Deploy/channel verification before more deep-link fixes.
- Decide whether "travel/fun" needs a first-class `StudyGoal` or reading/self-study is enough.
- Decide whether large-font/tablet users need explicit text-size and simplified-navigation settings before beta.
