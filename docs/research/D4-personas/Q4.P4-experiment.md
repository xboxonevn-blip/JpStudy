# D4.Q4.P4 Experiment - Bác Hùng N4

Commit: `e468d6c7`
Date: 2026-05-14
Live target: `https://jpstudy-v2.web.app`

## Setup

- Browser: Playwright Chromium.
- Tablet viewport: `834x1194` portrait.
- Large-font proxy: browser zoom approximately `125%` via `Ctrl++`.
- Pref seed. There is no `travel` study goal in code (`StudyGoal` is `jlpt|reading|writing`), so `reading` is the closest available low-pressure proxy.

```js
localStorage.clear();
localStorage.setItem('flutter.onboarding.completed', JSON.stringify(true));
localStorage.setItem('flutter.onboarding.level', JSON.stringify('n4'));
localStorage.setItem('flutter.onboarding.goal', JSON.stringify('reading'));
localStorage.setItem('flutter.app.locale', JSON.stringify('vi'));
localStorage.setItem('flutter.analytics.consent', JSON.stringify(false));
```

## Flow

1. A1 Root tablet cold start: open `/#/`; record level, language, plan, action density, and clipping.
2. A2 Root slow tap: tap one obvious non-destructive CTA; record whether route/action is predictable.
3. B1 Direct kanji: hard reload `/#/kanji`; record level, modal, target sizes, and content count.
4. B2 Direct vocab: hard reload `/#/vocab`; record level, lane, content count, and empty-state copy.
5. B3 Direct grammar: hard reload `/#/grammar`; record level, count, and CTA visibility.
6. C1 Reading/travel: open `/#/jlpt/reading` and `/#/study-hub` if available; record non-exam/travel suitability.
7. D1 Community/settings: open `/#/community`; record accessibility/settings/help affordances.

## Evidence

- Save screenshots to `tests/uat-p4-2026-05-14/`.
- Raw output in `Q4.P4-raw-output.md`.
- Analysis in `Q4.P4-analysis.md`.
- Session note in `docs/notes/2026-05-14-uat-bac-hung-n4-session.md`.

## Constraints

- No quiz submissions, no account mutations, no admin data export.
- Treat live deploy drift as production evidence; separate local-code fixes from live-channel issues.
