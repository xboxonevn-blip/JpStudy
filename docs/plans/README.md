# Plans

`docs/plans/` contains implementation plans that are still useful as active or near-active reference material.

## Subfolders

- `legacy/`: a very small set of still-useful historical references
- older low-priority legacy plans are moved to `docs/archive/legacy-plans/`

## Naming convention

Most newer plan files use a date-prefixed format such as:
- `YYYY-MM-DD-feature-plan.md`
- `YYYY-MM-DD-feature-design.md`

## Usage

- Put current planning documents here.
- Move outdated plan docs into `docs/plans/legacy/` instead of leaving them at the `docs/` root.
