# Next Execution Plan - 2026-03-19

## Summary
- This file is the active working contract after the repo-wide audit on `2026-03-19`.
- Baseline is green on local: `flutter analyze`, `flutter test`, and `flutter build web`.
- The next active milestone is `Grammar Practice hardening`.
- `Immersion` is now maintenance-only unless a new regression appears.

## Current Status
- Completed in this execution cycle:
  - saved the plan + updated repo working-memory files
  - finished `Grammar Practice hardening`
  - regenerated `docs/reports/grammar-example-quality-report.json`
  - updated release-truth docs in `README.md` and `ROADMAP.md`
  - finished `Handwriting reliability` pass 1 with benchmark and regression updates
  - finished `Route smoke hardening` pass 1 for the core shell routes on wide layout
- Current baseline remains green after the latest hardening pass:
  - `flutter analyze`
  - `flutter test`
  - `flutter build web`
- Practical next focus after this file:
  - keep handwriting in maintenance mode unless new regressions appear
  - keep route smoke coverage growing only where a high-value screen is still unguarded
  - move to the next repo-wide hardening target instead of reopening `Immersion`

## Execution Order
1. Save this plan, append work log, and keep the active plan in-repo.
2. Keep the current worktree narrative intact: immersion cleanup, CI baseline, smoke coverage, and handwriting regression safety.
3. Harden `Grammar Practice` with a data-first flow:
   - use `grammar_examples` + quality assessor as the authority for which question types are allowed
   - keep canonical grammar fields Japanese-first
   - avoid generating unsupported repair/transformation questions
   - prioritize same-level and same-lesson distractors
   - randomize session pools and answer order
   - cap over-repetition of one grammar point inside a session
   - keep session state explicit in the UI and never preselect an answer on entry
4. Regenerate and use `grammar-example-quality-report.json` as the canonical post-pass report.
5. Continue the handwriting reliability pass without adding new modes:
   - prefer one geometry pipeline
   - reduce false negatives and false positives
   - keep `Next`, completion flow, and session scope stable
6. Update release-truth docs so `README.md` and `ROADMAP.md` reflect the current repo state.

## Required Gates
- `flutter analyze`
- `flutter test`
- `flutter build web`

## Key Assumptions
- Mobile and web still share one core implementation.
- No cloud sync or large new feature work starts before Grammar Practice and Handwriting reliability are steadier.
- Legacy snapshots such as `docs/reports/full-content-audit.json` remain non-canonical until regenerated by a current pipeline.
