# Local Unihan Sources

`Unihan.zip` must be downloaded manually from https://www.unicode.org/Public/UCD/latest/ucd/Unihan.zip.

Extract the archive into `.codex/sources/Unihan/` before running `tooling/apply_unihan_upper_kanji_metadata.py`.

The zip file and extracted directory are gitignored to avoid bloating the repository.
